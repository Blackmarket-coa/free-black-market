# Audit Debt Tracker

Deferred work captured from prior audits and direct repo inspection. **Items here are intentionally not being fixed in the current production-readiness pass** — they are tracked so they can be ratcheted down in dedicated follow-up PRs without losing visibility.

Effort key: **S** ≤ 1 day · **M** 2–5 days · **L** > 1 week.

## Deferred-workstreams pass (backend feature seams)

Closed the four open backend feature seams tracked across the roadmaps
(`docs/CREATOR_COMMERCE_ROADMAP.md`, `HANDOFF.md`, in-code TODO markers). Each
new live path ships dark behind its env flag; DB/Stripe/OAuth paths are
CI/live-env verified.

| # | Item | Resolution | Location |
|---|------|------------|----------|
| ~~DW-1~~ | Subscription renewal order-creation no-op (Creator-Commerce Slice A) | **done** — renewal workflow mints the order + authorizes an off-session payment + links it + grants entitlements, gated by `FBM_SUBSCRIPTION_RENEWAL_LIVE`; pure shaping unit-tested | `backend/src/workflows/subscription/{renew-helpers.ts,workflows/renew-subscription.ts}` |
| ~~DW-2~~ | Content-platform OAuth adapters throwing `pending impl` | **done** — TikTok/Instagram/YouTube/Twitch implement real OAuth + metrics via a shared `providers/http.ts`; mocked-fetch unit tests. `blackout.ts` stays stubbed (awaiting Blackout API spec) | `backend/src/modules/content-platform/providers/*` |
| ~~DW-3~~ | Dark Blackout emitters (§2/§3) never invoked | **done (partial by design)** — `referral.attributed` (attribute-order-on-placed) + `ledger.usdc_converted` (hawala-settlement) wired; the other four stay stubbed with documented blockers (no order-failed/chargeback/quest/ambassador concept) | `backend/src/lib/blackout-wire-helpers.ts`, `subscribers/attribute-order-on-placed.ts`, `jobs/hawala-settlement.ts` |
| ~~DW-4~~ | Creator-Commerce Phase 2A/2B | **done** — plugin install/entitlement-verification API (2A) + `service-program` reviews model/endpoints (2B). ~~Deferred tails: plugin hook registry + semver~~ (both landed: hooks via `/v1/seller/plugins/:slug/hooks` + `plugin.*` events; the host-compat gate via `plugin-registry/compat.ts` — and W3 finished versioning with `plugin_version` history + prerelease-aware precedence). Remaining tail: service messaging hook | `backend/src/api/store/plugins/**`, `backend/src/modules/{plugin-registry/entitlement.ts,service-program/**}` |

## W1b — Blackout billing consolidation (2026-08-29)

Closed in the W1b pass (see `docs/contracts/blackout-integration.md` §"Blackout
checkout"): the checkout-session stub is now a real stateful purchase path
(idempotency row + shadow product + cart/payment/subscription), entitlement
Bug A (expiry never extended on renew), Gap C (`payment_method_id` never
persisted), Gap D (renewal ledger legs untyped — now
`reference_type=SUBSCRIPTION_RENEWAL`), Gap E (cancel/expire never revoked
subscription grants), and Ambiguity B (`customer_external_id` carried the
OAuth sub — now mxid-only).

| # | Item | Status | Location |
|---|------|--------|----------|
| W1B-1 | No HTTP integration spec for `/v1/integrations/blackout/**` (checkout happy-path against Postgres) — unit specs cover session idempotency, tier mapping, grant extension, and the emitters; the app-boot + migration surface is exercised by the Integration Tests CI job | **deferred (S)** | `integration-tests/http/` |
| W1B-2 | Off-session renewal depends on the first checkout attaching a reusable Stripe payment method (`setup_future_usage` passed through the payment session `data`); must be verified in the Stripe dashboard during go-live acceptance — no live key exists pre-launch to verify sooner | **go-live acceptance step** | `backend/src/api/v1/integrations/blackout/commerce/checkout/sessions/[token]/page/route.ts` |
| W1B-3 | Gift subscriptions stay local-only on Blackout (`comped` override); a paid gift rail would hold stored value and violate Posture-A no-balance-holding — revisit only as an immediate-grant purchase | **deferred by design** | blackout `services/subscriptionGifts.ts` |
| W1B-4 | Prorated refunds: refund of a subscription order cancels the sub + revokes the bundle (minimal handling); pro-rata credit is out of scope | **deferred (M)** | `backend/src/subscribers/revoke-entitlements-on-refund.ts` |

## W2 — Identity / MAS (2026-08-29)

Landed dark in the W2 pass (see `docs/contracts/mas-identity-consumer.md` and
the canonical `blackout/docs/contracts/mas-identity.md`): the `mas` Medusa
auth provider, `MAS_OIDC_*` gating, and OIDC-mxid precedence in the
customer-created subscriber. Deliberate deferrals:

| # | Item | Status | Location |
|---|------|--------|----------|
| W2-1 | Seller OIDC actor (`/auth/seller/mas`) — would bypass vendor registration/approval; sellers keep emailpass/Google until an OIDC-initiated onboarding design exists | **deferred by design** | `backend/src/lib/build-auth-module.ts` |
| W2-2 | Embedded-chat `mintLoginToken` under MSC3861 — admin-impersonation login is expected to break when Synapse delegates auth to MAS; verify on the staging Mode-B flip and design the OIDC-native handoff if so | **operator verification item** | `backend/src/shared/matrix-service.ts` |
| W2-3 | `seller_metadata.mxid` backfill from OIDC identities (vendors who later link a MAS login) — customer metadata only for now | **deferred (S)** | `backend/src/subscribers/customer-created-matrix.ts` |
| W2-4 | Blackout client `/auth/callback` landing page (the SDK + `AuthDelegatedLoginPage` are wired; the callback page has no reachable backend until the operator's staging flip) | **deferred until Mode B staging** | blackout `apps/blackout-client` |

## W3 — Registry + Forge MVP (2026-08-29)

Landed dark in the W3 pass (see `docs/contracts/extension-manifest.md`): the
shared extension manifest + validators, the seller publish bridge into the
plugin registry, `plugin_version` history, signature verification + the
publishing-keys endpoints, registry surface closure (detail/manifest routes,
uninstall on both surfaces, seller-scoped plugin entitlements, deprecation),
and the Featured Vendor Widget first-party seed. Deliberate deferrals:

| # | Item | Status | Location |
|---|------|--------|----------|
| ~~W3-1~~ | ~~The §5 commerce-API publish path (`/v1/integrations/blackout/commerce/seller/listings/[id]/publish`) can flip an extension listing to PUBLISHED with no signature and no registry row — reject extension-marked listings there or port the bridge~~ — **fixed 2026-09-10 by rejecting.** Porting would mean two copies of a security-critical flow (manifest validation, plugin-slug ownership, duplicate-version-different-artifact check, signing, registry bridge), free to diverge; the seller API remains the only route that can publish an extension, and the commerce route now 409s `extension_publish_unsupported` with a pointer to it. Both markers are covered — `plugin_slug` and a manifest carrying `artifactKind` — via the same `isExtensionListing` predicate the seller route uses, so the two cannot drift on what counts as an extension. **A second defect in the same three lines, not named by this row:** the route stamped `signed_at` while producing no signature, leaving an unsigned listing indistinguishable from a signed one to any later audit. It no longer stamps it. | done | `backend/src/api/v1/integrations/blackout/commerce/seller/listings/[id]/publish/route.ts` |
| W3-2 | Signing-key rotation: the publishing-keys document is single-key (derived from the private key); retired-keys support needs an additional env + doc append | **deferred (S)** | `backend/src/modules/marketplace-signing/verify.ts` |
| W3-3 | `creator_listing.compatible_with` stays dead — superseded by the manifest `fbm.*` block; consider dropping the column | **deferred (S)** | `backend/src/modules/marketplace-listing/models/creator-listing.ts` |
| W3-4 | Seller-scoped `plugin:<slug>` entitlement grant is best-effort; `seller_metadata.enabled_extensions` stays authoritative — hardening to authoritative needs a backfill + failure semantics | **deferred (M)** | `backend/src/api/v1/seller/plugins/[slug]/install/route.ts` |
| W3-5 | Integration-http spec for the publish → catalog → install loop against Postgres (unit/route harnesses cover the logic; mirrors W1B-1's posture) | **deferred (S)** | `integration-tests/http/` |
| W3-6 | Cross-repo counterparts — mostly landed same-window: Forge's publish flow consumes the seller surface (Forge W3 G-commits) and Blackout's provider consumes the registry read side (blackout W3 B1). Still open: Blackout featured-vendors client view (homepageCard.to target) + the client's pinned-key flip to the well-known keyset (operator/release item) | **other repos** | blackout `apps/blackout-client` |
| W3-7 | **The backend typecheck job cannot catch a whole class of type error.** `Lint & Type Check` runs `pnpm exec tsc --noEmit` in `backend/` with no `.medusa/` present, so `query.graph()`'s generated types — `RemoteQueryFilters` above all — do not exist and `filters` is loosely typed. A nested relation filter such as `filters: { orders: { id } }` passes that job and then fails `Build backend` with TS2322, minutes later in a different workflow. Found 2026-09-10 the hard way: it turned #852 red after a local `tsc --noEmit` came back clean. Nothing broken can merge, since `Build backend` is blocking — the cost is signal latency, not safety. The fix is to generate types before typechecking in that job, which today means running `medusa build` there (no standalone generate command exists in Medusa 2.14) and so adds ~35s to the fast job and duplicates work the Docker build already does. Left as a tradeoff to weigh rather than taken unilaterally; the trap is documented in `CONTRIBUTING.md` meanwhile. | **deferred (S)** | `.github/workflows/ci.yml`, `CONTRIBUTING.md` |

## W4 — Reputation consolidation (2026-08-30)

Landed dark in the W4 pass (decision D7): `recordKarmaEvent` as the canonical
reputation write path (source registry + attestation + idempotency; the
asset-graph reconciler converted), the xp_event→karma_event mirror job, the
reviews dedupe (`subject_type` on one model; service reviews absorbed; both
POST dialects accepted; GET added), the first producers (five-star reviews,
verification checks/badges), and the character-sheet karma projection fix
(`owner_id`→`member_id` — karma had never reached a sheet). Deferrals:

| # | Item | Status | Location |
|---|------|--------|----------|
| W4-1 | `@mercurjs/reviews` retirement: the plugin still owns the vendor-panel reviews screens (`/vendor/sellers/me/reviews` + reply/report) and `/admin/reviews`; the storefront's order `*reviews` relation also rides its order-review link, so FBM-written reviews don't mark an order as reviewed (customers see a 409 on the duplicate instead). Re-point the panels, then drop the plugin from `medusa-config.ts` — after checking the `review` table for rows worth migrating | **deferred (L)** | `backend/medusa-config.ts` plugins block; `vendor-panel/src/routes/reviews/` |
| W4-2 | karma_event append-only is service convention, not schema enforcement: the MedusaService-generated update/delete methods still exist and there is no DB trigger/revoked grant | **deferred (S)** | `backend/src/modules/hawala-ledger/karma.ts` |
| W4-3 | xp_event stays a separate table (the mirror is one-way); a full merge — xp reads served off karma_event projections — plus a created-at cursor for the sweep when the XP log grows hot | **deferred (M)** | `backend/src/modules/progression/karma-mirror.ts` |
| W4-4 | Karma attestations sign under the single marketplace key (rides W3-2's rotation debt); pre-W4 rows carry null attestations — a backfill would hash-stamp them without signatures | **deferred (S)** | `backend/src/modules/hawala-ledger/karma.ts` |
| W4-5 | A five-star review that is later edited down does not revoke its karma grant (append-only log; a compensating negative event needs an edit-path hook). **Not reachable today — verified 2026-09-10.** There is no review edit path anywhere in the API surface: `POST /store/reviews` is the only writer (the other seven review routes are all GET, bar one vendor POST), no `updateReviews`/`deleteReviews` caller exists outside the generated service, and `@mercurjs` ships no review route of its own. The row becomes live the day an edit or delete path lands, and what it will need then is a compensating **negative** event rather than a mutation — the log is append-only by design, and the existing grant is keyed `(source_module: "reviews", source_id: <review id>)`, so the reversal has to carry its own distinct source id or the dedup index will swallow it. Left open rather than pre-built: a hook for a path that does not exist cannot be tested against the shape that path will have. | **deferred (S), blocked on an edit path existing** | `backend/src/lib/karma-grants.ts`, `backend/src/api/store/reviews/route.ts` |
| W4-6 | Cross-repo counterparts: blackout hardens its per-context `reputation_events` log (actor/source attribution, insert-only persistence, standing endpoint off the attention metric — blackout W4 B1); Blackstar's `NodeTrustScore` stays frozen with the repo (its CONSOLIDATION.md records the projection direction); blackmask posture signals as karma inputs remain future work | **other repos** | blackout `packages/api`; Blackstar; blackmask |

## W5 — Geospatial service (2026-08-30)

Landed dark in the W5 pass (decision D5 — Blackout is the spatial home; see
`docs/contracts/blackout-spatial-consumer.md`): six duplicated haversines
consolidated into `lib/geo-distance.ts`; the two divergent ZIP3 tables
(backend ~900 entries vs storefront ~180 — checkout could fail to geocode a
ZIP the vendors page resolved) collapsed into `lib/zip3.ts` behind the new
`GET /store/geocode`; and the `blackout-spatial` consumer wired remote-first
behind `FBM_BLACKOUT_SPATIAL` with the ZIP3 fallback always available.
Deferrals:

| # | Item | Status | Location |
|---|------|--------|----------|
| W5-1 | Nearby-within-radius as a remote call needs Blackout to hold/mirror FBM's vendor coordinates (per-row remote calls on the debounced public search path are non-viable; a single candidate-set call requires the data). Data-ownership decision, then a Blackout endpoint + consumer | **deferred (L)** | `backend/src/api/store/vendors/route.ts`, blackout `/v1/spatial` |
| W5-2 | Delivery-zone containment stays local (checkout path — a remote dependency there needs a circuit breaker + the zone geometries mirrored); the ray-cast + bbox approximation in the zone routes is the weakest spatial code in the repo | **deferred (M)** | `backend/src/api/store/delivery-zones/**`, `backend/src/api/vendor/delivery-zones/conflict-utils.ts` |
| W5-3 | `lib/aid-location.ts` distance work stays local BY DESIGN — mutual-aid coordinates never leave the server (privacy contract, `docs/FBM_BUYER_HUB.md`). Recorded as a permanent scope exclusion, not debt to burn down | **excluded** | `backend/src/lib/aid-location.ts` |
| W5-4 | Metadata-backed vendors carry `location: {}` so the distance filter silently drops every non-producer/kitchen/garden archetype when a location filter is active; fixing it makes them newly appear in filtered results (visible product change — decide + announce) | **deferred (M)** | `backend/src/api/store/vendors/route.ts` |
| W5-5 | `vendor-rules.max_delivery_distance` is a dead stub ("Would calculate distance here") — implementing needs a geocoded delivery address, a natural follow-on once `blackout-spatial` is live | **deferred (S)** | `backend/src/modules/vendor-rules/service.ts` |
| W5-6 | Cross-repo counterparts: Blackout's deployed PostGIS + martin remain UNFED by its product (its OSS-plan WS4 owns the seam); the standalone `coalition-app` repo absorb/archive is an operator action | **other repos / operator** | blackout `infra/single-server-baseline`, `coalition-app` |

## W6 — Federation (2026-08-30)

The W6 pass found the roadmap's "version connect.js as a shippable artifact"
had already shipped 2026-08-13 (frozen `/v2.0.0/connect.js` + SRI + changelog
+ the `connect-sri.unit.spec.ts` parity gate — PRs #801–#803); W6 closed the
residue (Launch-template pin now spec-enforced; embed-key middleware specs)
and added the BMC↔UCP mapping page in The-Connect
(`docs/documentation/ucp-and-bmc-connect.md` there). What remains is small
and mostly decisions, not debt:

| # | Item | Status | Location |
|---|------|--------|----------|
| W6-1 | `@fbm/connect` npm package: REJECTED, not pending — `window.FBM` is the contract, the storefront has no bundler step for the SDK, and a second published artifact would be a permanent sync liability the parity spec would have to police, with no reachable user the pinned `<script>` tag doesn't serve. Revisit only if a partner materially cannot use a script tag | **decision** | `storefront/public/connect.js`, `backend/src/shared/website-config.ts` |
| W6-2 | Storefront middleware trap: SDK URLs survive the i18n country-redirect only because they contain a literal dot (`pathname.includes(".")` early-return). Any future extensionless alias (`/sdk/latest`) 307s every embed to `/us/…` unless the matcher is amended in the same change | **guidance** | `storefront/src/middleware.ts` (matcher + early-return) |
| W6-3 | The SRI parity spec reads `storefront/public/` and `templates/` from `backend/` via `path.resolve(__dirname, "../../../..")` — silently unrunnable under a sparse/split checkout of `backend/` alone | **accepted (monorepo)** | `backend/src/shared/__tests__/connect-sri.unit.spec.ts` |
| W6-4 | Mutable `/connect.js` still serves with up to a 24h stale-while-revalidate window — a bad mutable release has no rollback lever for embedders who ignore the docs and pin nothing; all first-party surfaces are now pinned, which is the mitigation | **accepted** | `storefront/next.config.ts` (headers) |
| W6-5 | The-Connect fork cannot run its own docs CI end-to-end in this container (`ucp-schema` install blocked at crates.io; no `gh-pages` branch for mike) — local gate is `DOCS_MODE=root mkdocs build --strict`; the fork's `docs.yml` may fail at its cargo-install step on fork infra, not on content | **other repo / infra** | The-Connect `.github/workflows/docs.yml`, `scripts/build_local.sh` |

## ⚠️ Critical: money-path atomicity was silently disabled (FIXED 2026-06-20)

**Severity: high (financial correctness).** Surfaced by the money-path concurrency
soak once it was made runnable in the production-readiness pass.

The "atomic-by-default" balance/counter CAS paths **never engaged in production**.
`HawalaLedgerModuleService.resolvePgConnection()` (and the equivalent in
`creator-attribution`) read `this.container_`, but Medusa's `MedusaService` base
stores the module container as `this.__container__` — `container_` is never set.
So `resolvePgConnection()` always returned `undefined`, and every
`createTransfer` / `createInvestment` / affiliate-counter increment silently fell
back to the **non-atomic read-modify-write** path. Under concurrency that allows
account overdraw and lost pool/counter updates (real money/value integrity risk).

The unit tests that "verified" the atomic feature mocked the same wrong
`container_` property, so they passed and masked the bug.

**Fix (this pass):**
- Corrected the container access to `__container__` in
  `backend/src/modules/hawala-ledger/service.ts` and
  `backend/src/modules/creator-attribution/service.ts` (supports both a
  container `.resolve()` and an awilix cradle, plus a MikroORM-manager-derived
  knex fallback for harnesses that don't register `PG_CONNECTION`).
- Updated the two atomic unit specs to mock `__container__` (they now validate
  the real path).
- The money-path concurrency soak (`hawala-ledger/__tests__/concurrency-soak`)
  now runs against live Postgres and passes 3/3: no overdraw under 150 concurrent
  debits on a 100-balance account, value conservation across bidirectional
  transfers, and exact investment-pool totals under concurrent investments.

**Follow-up to verify:** confirm in a full-app (not module-test) context that
`PG_CONNECTION` resolves so production takes tier-1 (registered knex) rather than
the manager fallback; audit any other module copying the `container_` pattern.

## Type-safety debt

| # | Item | Location | Count | Owner | Effort | Target milestone |
|---|------|----------|------:|-------|:------:|------------------|
| TS-1 | Replace `any` in API hooks, table/grid utilities, dashboard extensibility. Incrementally chipped via the LR-1 ratchet (Step 8 removed 249 redundant react-query callback anys; Step 10 typed the tenancy fetches; Step 11 typed the seller sort helpers + donations/onboarding endpoint responses + product map callbacks; Step 12 typed product-edit-variant option reduce/maps, the `isFetchError`/invite/reset-password type guards (`any`→`unknown`), the add-exchange sort values, and the ticket-product region forEach; Step 13 typed the attribute-list sort values, the ticket-product price object, the promotions rules-form-field callbacks, and the pricing-edit defaultValues reduce). Remaining ~352 are mostly load-bearing SDK-drift casts; closing this needs a dedicated SDK-typing sprint (per-entity table-adapter types + Medusa filter/column-metadata typings). **SDK-typing sprint started 2026-06-22** (slice 1): typed the `hooks/api` mutation-option generics (see LR-1 Step 14, 22 anys). Architecture note from the slice-1 exploration: the `TableAdapter<TData>` interface is *already* generic — its residual anys are localized (`params`, `error`, `apiColumns`), and the `table-display-utils`/`cell-renderers` anys are intentional value-dispatch polymorphism; so remaining clears are per-cluster (mutation/query option generics, entity DTO casts) rather than one generic thread. | `admin-panel/src/hooks/api/*.tsx`, `admin-panel/src/components/data-grid/*`, `admin-panel/src/lib/table/*`, `admin-panel/src/types/*`, `admin-panel/src/dashboard-app/*` | ~352 (was 671) | admin-panel team | L | post-`v1.0.0` |
| TS-2 | Replace `any` in pricing helpers, data access, return/shipping UI. **Partial 2026-06-22**: cleared the pure-helper/data clusters — `get-product-price.ts` + `get-seller-product-price.ts` (modelled the variant/price shapes the code reads; hoisted nullable fields into coalesced locals), `isEmpty.ts` (`any`→`unknown`), `compare-addresses.ts` (`any`→`object`), `data/regions.ts` (`catch (e: any)`→bare catch), `data/orders.ts` (`Record<string, any>`→`unknown`), and the `data/cart.ts` `setAddresses` cast (see TD-5). **Partial 2 (2026-06-22):** cleared the return/shipping component clusters (~49 anys) — added `storefront/src/types/order-return.ts` (custom `ReturnRequest`/`ReturnLineItem`/`SelectedReturnItem`/`ReturnRequestPayload`/`ReturnOrder`/`OrderLineItemWithReason` modelled from usage, reusing `HttpTypes.Store*` + `SellerProps`), typed the `OrderReturnSection` tabs + `SingleOrderReturn`/`OrderReturnRequests`/`OrderReturn`/`OrderParcelActions`, typed `data/orders.ts` return fns (`createReturnRequest`/`getReturns`/`retriveReturnMethods`) and widened `retrieveOrder` to `ReturnOrder` (dropping two `as any` casts at the return page), typed `CartShippingMethodsSection` (`ShippingMethodRule`/`AvailableShippingMethod`, reduce generic, callback params, `catch`), and `data/cart.ts` (tier fetch generic — also fixed a double-`JSON.stringify` body bug, `StoreCartPromotion`, `catch`); extended `SellerProps` with `state`/`phone`. Typecheck + tests + lint clean. **Remaining:** `data/orders.ts` `retrieveOrderSet` (custom `order_set` endpoint, no SDK type) and `listOrders` seller `reviews` (order-list/reviews cluster), plus any stray component anys outside these two sections. **Partial 3 (2026-06-22):** cleared ~26 more in low-risk clusters — `lib/listing/policy.ts` + `lib/listing/unified-products.ts` (modelled local `ProductWithSellerFields`/`PricedVariant`/`FacetProduct`/`FacetVariant` extension types instead of `product as any` + untyped variant/category/channel callbacks), `lib/config.ts` + `data/customer.ts` catch clauses (`any` → typed `unknown` casts, incl. `getErrorMessage`), `components/cells/OrderCancel` (StoreOrder/StoreOrderLineItem), `organisms/ShippingAddress` form state (`Record<string,string>`), and `helpers/isEmpty.ts`. Typecheck + 226 tests + lint clean. **Partial 4 (2026-06-23):** cleared the review-array cluster (9 anys) with a coordinated shared type — added `storefront/src/types/review.ts` (`OrderReview` for the `/store/orders`+`/store/reviews` `*reviews` relation, `CreateReviewPayload` for `POST /store/reviews`), pointed `SellerProps.reviews` + `SellerReviewList` at the existing `SingleProductReview`, typed `data/{reviews,orders}.ts` `Order`/order-list review arrays + `createReview`, and de-duplicated the inline `Order` shape in `ReviewsToWrite`. Typecheck + 226 tests + lint clean. **Remaining (~36):** the cascade-prone clusters deliberately deferred — `ProductCard` Algolia `Hit` union casts, `data/discovery.ts` custom endpoints (no backend schema), `CartReview/CartItems` seller-grouping (runtime seller shapes don't satisfy `SingleProductSeller`), and `data/orders.ts` `retrieveOrderSet`. | `storefront/src/lib/listing/{policy,unified-products}.ts`, `storefront/src/lib/{config.ts,data/customer.ts}`, `storefront/src/components/cells/OrderCancel/*`, `storefront/src/components/organisms/ShippingAddress/*` | ~36 (was 158) | storefront team | M | `v1.1.0` |
| TS-3 | Replace `any` in backend runtime so `@typescript-eslint/no-explicit-any` can be flipped from `off` → `error` in `backend/eslint.config.mjs`. Parked when the backend ESLint flat config landed (LR-4); the rule is the single high-volume blocker to a stricter backend lint. **Enforcement ratchet started 2026-06-22**: rather than a one-shot flip of ~1,215 anys (infeasible; most are load-bearing MikroORM/Medusa-container casts), the flat config now carries a **per-directory override that sets `no-explicit-any: error` for already-cleared subtrees** (mirrors the admin `--max-warnings` ratchet). First cleared + gated: `src/api/{users,shared,webhooks,middlewares}` — converted `catch (e: any)` → typed `unknown`, `(req as any).rawBody`/`auth_context`/`storefront_context` → typed casts, container `resolve("…")` → typed generic, and ORM `updateData: any` → inferred objects with `as const` status literals. Verified the gate bites (a probe `any` fails `--max-warnings 0`) and reverts clean. **Grown 2026-06-23**: cleared the last 7 anys in `src/api/{deliveries,restaurants}` — the `isDeliveryRestaurant`/`isDeliveryDriver` middleware `as any` casts were simply dropped (the functions are already assignable to `defineMiddlewares`' `MiddlewareFunction[]`), the workflow-subscribe handler `(data: any)` → `(data: unknown)`, and the restaurant-products workflow input `as any[]` → `as CreateProductWorkflowInputDTO[]` — then **added 10 directories to the override block**: the two just-cleared (`deliveries`, `restaurants`) plus eight already-any-free dirs gated for free regression protection (`auth`, `delivery-zones`, `health`, `key-exchange`, `product-feed`, `r`, `tickets`, `utils`). Gate now covers 14 dirs; re-verified it bites + reverts clean, full backend lint `--max-warnings 0` green, 788 unit tests pass. **Grown again 2026-06-23 (v1 marketplace slice)**: cleared 25 anys across `src/api/v1/{admin,checkout,integrations,marketplace,webhooks}` and gated those 5 subtrees. Patterns: `order: { … } as any` → `as const` (6×, list-method options); `(jwt.sign/verify as any)(…)` → direct typed calls (4×, one needed `interface SessionTokenPayload` → `type` so the payload satisfies `Record<string, unknown>`); `req.scope.resolve<any>(MODULE)` → typed service generic (2×); redundant `.map`/`.filter((x: any))` annotations dropped (now contextually typed, 5×); `toPublicListing(listing: any)` → `Awaited<ReturnType<…listCreatorListings>>[number]`; `(req as any).rawBody` → typed cast; `eventType: "resolved" as any` → `SubcontractEventType.RESOLVED`; and unnecessary `(service as any).{delete,update}CreatorListings`/`updateOrderSubcontracts` casts removed (the generated MedusaService methods are on the typed service), with `createCreatorListings` kept behind a precise local method-signature cast (no `any`) since its generated input type has drifted from the route payload. **Surfaced + fixed a latent bug**: the two creator routes called the non-existent singular `service.listSellerMetadata(...)` (the generated method is `listSellerMetadatas`, used correctly in 4 other call sites) — `resolve<any>` had masked it, so those endpoints were 500-ing; corrected to the real method. Gate now covers 19 dirs; re-verified it bites + reverts clean, lint `--max-warnings 0` green, 788 unit tests pass. **Big chunk 2026-06-23 (admin/** + v1/seller/** trees)**: cleared **155 anys** across the `admin` and `v1/seller` API trees and switched the gate to **whole-tree globs** (`src/api/admin/**` + `src/api/v1/seller/**`) with a small companion `off` override for the 4 subtrees still carrying load-bearing anys (`admin/auth-debug`, `admin/cms-categories`, `admin/vendor-hype`, `v1/seller/programs`). Dominant transforms (tsconfig is non-strict, so stripping a redundant `: any` annotation yields implicit-any with no type change): dropped redundant `(x: any)`/`catch (e: any)`/comparator param annotations; `const service: any = resolve(X)` / `resolve<any>(X)` → typed `resolve<Service>(X)` (asset-graph, cooperative, vendor-verification, seller-extension) + dropped the now-redundant `(service as Service)` casts; `Record<string, any>` → `Record<string, unknown>`; redundant `(x as any[])` iteration casts dropped; `order: { … } as any` → `as const`; `(req as any).storefront_context`/`auth_context` + `(x.metadata as any)` → typed casts; `new Map<string, any>()` → `new Map<string, (typeof scores)[number]>()`; `req.body as any` destructures → precise inline shapes; variable annotations (`savings`/`post`/`productInput`/`contract`) → `unknown`/`Record`/derived/`Awaited<ReturnType<…>>` types; and generated-method input casts (`create*`/`update*`) with drifted input types kept behind precise local method-signature casts (no `any`). **Surfaced + fixed a latent bug earlier** (creator routes' `listSellerMetadata` → `listSellerMetadatas`). Gate now covers 19 explicit dirs **plus the admin/** & v1/seller/** trees** (4 subtrees deferred); re-verified it bites on a newly-gated dir, the off-override exempts deferred dirs, typecheck 0 errors, lint `--max-warnings 0` green, 788 unit tests pass. **Big chunk 2026-06-23 (vendor/** tree)**: cleared **259 anys** across `src/api/vendor` and gated the whole tree (`src/api/vendor/**`) with `off` exceptions for 4 load-bearing subtrees (`deliveries`, `farm`, `me`, `printful`) and the unit-test mock casts (`**/__tests__/**`, `**/*.spec.ts`). The dominant cluster was **`(req as any).{auth_context,_seller_id,_sellerContextResolved}` (77 sites)** → a new shared `src/api/vendor/types.ts` `VendorRequest = AuthenticatedMedusaRequest & { _seller_id?; _sellerContextResolved? }` cast threaded across 45 files. Plus: `catch (e: any)`/callback/comparator annotation strips (non-strict tsconfig → implicit-any, no behavior change); `Record<string, any>` → `unknown`; `<svc> as any` at the `{create,update}SellerMetadataRecord` helper calls → `as SellerExtensionService`; `strictRateLimiter as any` in `defineMiddlewares` arrays → `as MiddlewareFunction`; typed `resolve<PlaybookService/SellerExtensionService>`; dropped redundant `(service as any).{update}AffiliateLinks` casts; `req.body as any` product destructures typed + `productData as CreateProductWorkflowInputDTO` at the workflow boundary; `status as ProductStatus`; `activeZones as unknown as ExistingZone[]` (exported the local type); and return-only variable annotations (`bargainingProposal`/`createdProducts`/`ticketProducts`/`poolInput`) → `unknown`/`Record`/precise shapes. Gate now covers the admin/**, v1/seller/**, **and vendor/** trees** (8 subtrees + vendor test files deferred); re-verified it bites + the off-override exempts deferred dirs, typecheck 0 errors, lint `--max-warnings 0` green, 788 unit tests pass. **To grow**: clear the deferred subtrees and `src/api/store`, then move beyond `src/api` (modules/workflows/subscribers). | `backend/eslint.config.mjs`, `backend/src/api/**` (admin + v1 + vendor trees; deferred: `admin/{auth-debug,cms-categories,vendor-hype}`, `v1/seller/programs`, `vendor/{deliveries,farm,me,printful}` + vendor tests) | ~1,180 (admin + v1/seller + vendor trees gated) | backend team | L | post-`v1.0.0` |

## In-code TODO/FIXME debt

| # | Item | Location | Count | Owner | Effort | Target milestone |
|---|------|----------|------:|-------|:------:|------------------|
| TD-1 | Resolve admin-panel TODO/FIXME backlog (see `TODO_TRACKER.md`). **Triaged 2026-06-22**: admin-panel is a vendored `@medusajs/dashboard@2.14.2` fork; all 14 markers are upstream, Low severity. Fixed the 2 that were safe to change in-fork — claim-create-form **ESC-to-cancel** (keydown listener sets `IS_CANCELING` so Escape-close cancels the in-progress claim like the Cancel button) and product-variant-create **tab-dirty detection** (`isTabDirty` via `form.formState.dirtyFields` keyed by each tab's field set, so a touched tab keeps its progress on backward nav). The remaining **12 are deferred to backend/upstream** (no bulk allocate endpoint; RQ `*address` join gap; no `region_id`/`order` on the relevant Medusa APIs; action-log + sales-channel response shapes; option-ID constraints) or are **upstream component / i18n-architecture** decisions (data-grid undo/redo focus, sticky table header, `use-date` locale-vs-language) — each row in `TODO_TRACKER.md` now carries a Triage rationale rather than being patched in the fork. Typecheck + lint (488) + tests clean. | `admin-panel/src/routes/orders/order-create-claim/**`, `admin-panel/src/routes/products/product-create-variant/**`, `TODO_TRACKER.md` | 12 (was 14) | admin-panel team | M | `v1.1.0` |
| ~~TD-2~~ | ~~Order list pagination~~ — **resolved**: pagination implemented at `storefront/src/app/[locale]/(main)/user/orders/page.tsx:33-94` (LIMIT=10, offset-based slicing, `<OrdersPagination />` render). Verified 2026-05-13. | `storefront/src/app/[locale]/(main)/user/orders/page.tsx` | 0 | — | — | done |
| ~~TD-3~~ | ~~Sell signup → API integration~~ — **resolved 2026-05-13**: new `sell-signup` backend module (`backend/src/modules/sell-signup`) with a `sell_signup` table (`email`, `store_name`, `selling jsonb`, `status enum`, source_ip / user_agent / referer for spam triage, `status` enum drives the admin lifecycle `new → contacted → converted/rejected`). New `POST /store/sell-signup` endpoint validates the body with zod, persists, and returns `{id, status}` with 202. `storefront/src/app/api/sell-signup/route.ts` now forwards captures to the backend (propagating IP / UA / referer); failures are still swallowed so the client redirect to the vendor-panel registration page never blocks. 611 backend unit tests still pass; both `pnpm typecheck` runs pass. | `backend/src/modules/sell-signup/**`, `backend/src/api/store/sell-signup/route.ts`, `backend/medusa-config.ts`, `storefront/src/app/api/sell-signup/route.ts`, `storefront/src/app/[locale]/(main)/sell/SellPageClient.tsx` | 0 | — | — | done |
| ~~TD-4~~ | ~~Order details status source~~ — **resolved**: status sourced from `order.fulfillment_status` / `order.payment_status` on `HttpTypes.StoreOrder` at `storefront/src/components/organisms/OrderDefails/OrderDetails.tsx:33,42`. Verified 2026-05-13. | `storefront/src/components/organisms/OrderDefails/OrderDetails.tsx` | 0 | — | — | done |
| ~~TD-5~~ | ~~Cart data shape (POJO instead of form entity)~~ — **resolved 2026-06-22**: `setAddresses` now builds a typed `StoreUpdateCart` payload (shipping + billing) instead of casting to `any`; FormData entries are coerced to strings via a `toFormString` helper. Removing the `any` surfaced two latent type bugs in `CartAddressSection` (the `useActionState` initial state was the `sameAsBilling` boolean rather than the message string; the `ErrorMessage` `error` prop could be `false`) which were fixed. Storefront typecheck clean. | `storefront/src/lib/data/cart.ts`, `storefront/src/components/sections/CartAddressSection/CartAddressSection.tsx` | 0 | — | — | done |

## Logging hygiene

| # | Item | Location | Count | Owner | Effort | Target milestone |
|---|------|----------|------:|-------|:------:|------------------|
| ~~LG-1~~ | ~~Replace `console.*` with structured logger in backend runtime~~ — **resolved 2026-06-13**: 503 runtime `console.*` calls across 188 files migrated to the existing structured logger `backend/src/shared/logger.ts` (`createLogger`), whose methods were widened to a console-compatible variadic signature (`(message?, ...rest)`) so the migration was a clean level-mapped swap (`log/info`→`info`, `warn`→`warn`, `error`→`error`, `debug`→`debug`); trailing args fold into structured context and a leading `Error` is promoted to the structured `error` field. Excluded by design: `backend/src/scripts/**` (CLI/seed output) and tests. Backend typecheck 0 errors; unit suite green (733 tests; `posture-a-invariants` updated for the single-string log shape). | `backend/src/**` (excl. `scripts/**`, tests) | 0 | — | — | done |
| ~~LG-2~~ | ~~Replace `console.*` in storefront request wrappers and middleware~~ — **resolved 2026-06-13**: 56 calls across 34 files routed through the **existing** gated `storefront/src/lib/logger.ts` (`import { logger } from "@/lib/logger"`); imports inserted after any `"use client"`/`"use server"` directive. `pnpm typecheck` + `pnpm lint` clean. | `storefront/src/**` | 0 | — | — | done |
| ~~LG-3~~ | ~~Replace `console.*` in admin-panel runtime~~ — **resolved 2026-06-13**: new `admin-panel/src/lib/logger.ts` (gated, mirrors the storefront logger); 26 calls across 18 files migrated via `@lib/logger` (the `__tests__` translation spec's diagnostic `console.error` left intentionally). Typecheck 0 errors; ESLint 912 warnings (unchanged baseline, under the 1000 cap). | `admin-panel/src/**` (excl. tests) | 0 | — | — | done |

A single repo-wide regression guard `scripts/check-no-console.mjs` (`pnpm check:no-console`, wired into the CI `lint` job) now fails on any bare `console.*` in runtime code, excluding each app's logger file, `backend/src/scripts/**`, and tests. Chosen over three separate ESLint `no-console` rules to keep one mechanism across all three apps and avoid disturbing admin-panel's `--max-warnings 1000` ratchet (LR-1).

## Lint/typecheck baseline ratchet

| # | Item | Location | Current | Owner | Effort | Target milestone |
|---|------|----------|--------:|-------|:------:|------------------|
| LR-1 | Lower admin-panel ESLint `--max-warnings` from 7000 → 0 in steps (~~5500~~ → ~~4000~~ → ~~1000~~ → 0). **Step 1 done 2026-05-13**: ratcheted 7000 → 5500 after `pnpm exec eslint --fix` cleared 1,586 auto-fixable warnings (mostly `@typescript-eslint/consistent-type-imports` and `newline-before-return`). **Step 2 done 2026-05-13**: ratcheted 5500 → 4000 after adding the conventional `^_` ignorePattern to `@typescript-eslint/no-unused-vars` plus targeted cleanup. **Step 3 done 2026-05-13**: ratcheted 4000 → 1000 by landing an alias-rewrite codemod (`admin-panel/scripts/rewrite-imports-to-aliases.mjs`) that converted all relative `../` and sibling-traversing `./<aliased-dir>/…` imports across `src/**` to the TS path aliases declared in `tsconfig.json` (`@components`, `@hooks`, `@routes`, `@utils`, `@assets`, `@styles`, `@lib`, `@providers`, `@custom-types`, `@/*` catch-all). 4,508 imports rewritten across 980 files in two codemod passes; typecheck still 0 errors; `vite build` succeeds. Lint count: 3,985 → **912** (cap now 1000). The remaining 912 warnings break down to **681 `@typescript-eslint/no-explicit-any`** (most are load-bearing casts around Medusa SDK type drift introduced during LR-3), **82 `react-hooks/exhaustive-deps`**, **29 `@typescript-eslint/no-non-null-asserted-optional-chain`**, and ~120 small clusters (jsx-a11y, no-empty-object-type, etc.). **Step 4 (partial) done 2026-06-15**: ratcheted cap 1000 → **889** (tight, no buffer) by clearing 23 unambiguously-safe non-`any` warnings — 12 `@typescript-eslint/no-empty-object-type` (empty `interface X extends Y {}` → `type X = Y`; `{}` type args → `object`/`PropsWithChildren`), 3 `no-case-declarations` (block-wrapped `case`), 2 `react/jsx-key`, 4 `react/no-unescaped-entities`, 2 `no-irregular-whitespace` (nbsp → `&nbsp;`). Typecheck still 0 errors; admin tests pass. Deliberately skipped: `no-explicit-any` (681, load-bearing per LR-3), `react-hooks/exhaustive-deps` (82, behavioral risk), `no-non-null-asserted-optional-chain` (29, intent-dependent), `no-restricted-imports` (3, false-positives on dashboard-app-local `./routes/*`). **Step 5 (partial) done 2026-06-15**: ratcheted cap 889 → **833** by clearing 56 more verified-safe warnings — 29 `no-non-null-asserted-optional-chain` (`entity?.id!` → `entity?.id ?? ""` for hook/SDK args gated by `enabled`/mutations; redundant `!` removed where downstream already optional-chains), 14 `no-unused-expressions` (`cond && (await fn())` → `if (cond) { await fn() }`; `a ? f() : g()` → `if/else`; comma-operator → statements; `cond && cb()` → `cb?.()`), 4 `no-useless-escape` (dead `\[` / `\/` in a char class), 3 `ban-ts-comment` (`@ts-ignore` → `@ts-expect-error` w/ description, or removed when it suppressed nothing), 5 `newline-before-return`, 1 `no-extra-semi`. Typecheck still 0 errors. Remaining 833. **Step 6 (partial) done 2026-06-15**: ratcheted cap 833 → **779** by clearing 54 `jsx-a11y` warnings — 13 `no-autofocus` (justified disables; intentional initial focus on admin search/modal inputs), real keyboard handlers (`role="button"` + `tabIndex` + `onKeyDown` firing the click on Enter/Space) on ~14 genuinely clickable `<div>`/`<span>` toggles/selectors, 5 `anchor-is-valid` (Radix `Trigger asChild` `<a>` → focusable `<button type="button">`), and singletons (redundant `role` removed, `aria-selected` added to a `role="option"`, redundant "image" dropped from `alt`); stopPropagation/focus-forward/`<form>` wrappers got justified disables. Typecheck 0 errors; tests pass. The keyboard-handler additions are additive (mouse unchanged) but warrant a UI smoke-test. **Steps 7–8 done 2026-06-15**: cap 779 → **526**. Step 7 typed the 4 `catch (e: any)` clauses (`unknown` + `instanceof Error` narrowing). Step 8 (the big one) dropped the redundant `any` annotations on react-query callback params (`onSuccess`/`onError`/`onSettled` `(data, variables, context)`) across the API hooks so TanStack Query infers them from the typed `useMutation` — purely removing annotations (behavior-identical), typecheck-gated with 0 errors, clearing 249 `no-explicit-any`. A blanket `Record<string, any>` → `unknown` sweep was attempted and reverted (cascading type errors — the casts are load-bearing). Remaining 526: ~428 `no-explicit-any` (mostly hand-typed param/cast/annotation sites needing real types), 82 `react-hooks/exhaustive-deps` (behavioral), 4 `data-grid-root` a11y, 7 ambiguous `no-unused-vars`, 3 false-positive `no-restricted-imports`. **Step 9 done 2026-06-20** (production-readiness pass): ratcheted cap 526 → **521** by silencing the 5 confirmed false-positives with justified inline disables — 3 `no-restricted-imports` (dashboard-app's own `./routes/*` imports in `dashboard-app/index.ts` + `dashboard-app/dashboard-app.tsx`; no `@` alias applies to the dashboard-app-local dir) and 2 `react/prop-types` (`keybound-form.tsx`; props are fully typed via the `React.FormHTMLAttributes` generic, rule false-positives on `forwardRef`). Typecheck still 0 errors; admin tests pass. Deliberately left: 428 `no-explicit-any` (load-bearing SDK casts), 82 `exhaustive-deps` (behavioral), 4 data-grid a11y (keyboard-handler risk), 7 ambiguous `no-unused-vars` (schema consts only referenced via `typeof`; removing breaks the type). **Step 10 done 2026-06-22**: ratcheted cap 521 → **513** by typing the admin tenancy fetches — added a shared `TenancyOrganization`/`TenancyStorefront` type module (`admin-panel/src/lib/tenancy/types.ts`) and used it for the `/admin/tenancy/organizations` + `/admin/tenancy/storefronts` `sdk.client.fetch` generics and the `StorefrontSwitcher` props across `onboarding-checklist`, `donations`, and `donations-report` (6 `any[]`), plus the storefront `metadata` access in the donations page (2 `as any`). Typecheck still 0 errors; admin tests pass. Remaining 513: ~420 `no-explicit-any` (load-bearing SDK-drift casts), 82 `exhaustive-deps` (behavioral), 7 `no-unused-vars`, 4 a11y — the documented floor pending the dedicated SDK-typing sprint (TS-1). **Step 11 done 2026-06-22**: ratcheted cap 513 → **488** by typing 25 genuinely-safe `no-explicit-any` sites (typecheck-gated, 0 errors) — in `hooks/api/sellers.tsx` the 3 internal sort helpers (`AdminOrder[]`/`AdminProduct[]`/`AdminCustomerGroup[]`), 3 redundant filter-callback annotations, and 6 redundant `dateFilter` `as any` casts (the `filters` param stays `any` to avoid a caller cascade; the customer-group date filters use a concrete `{ $gte?; $lte? }` cast since that hook's `filters` is `Record<string,string|number>`); a new `admin-panel/src/types/donations/common.ts` typing the donations/onboarding endpoint responses (`beneficiaries`, `templates`, `settings`, `report`, onboarding `state`, import result/errors) consumed in `routes/{donations,donations-report,onboarding-checklist}`; and redundant map-callback annotations in `edit-product-option-form`, `product-create/utils.ts`, and `product-create-variant/details-tab`. A `pricing-edit.tsx` Object.entries destructure was attempted and reverted (the `: any` masked a `value` that can be `undefined`). Remaining 488: ~395 `no-explicit-any`, 82 `exhaustive-deps`, 7 `no-unused-vars`, 4 a11y. **Step 12 done 2026-06-22**: ratcheted cap 488 → **476** by clearing 12 more genuinely-safe `no-explicit-any` (typecheck-gated, 0 errors) — `product-edit-variant-form` option `reduce`/maps (`AdminProductOption`/`AdminProductVariantOption` inference + `Record<string,string|undefined>` accumulator), the `isFetchError` + invite + reset-password type guards (`any`→`unknown`), the `add-exchange-inbound-items-table` sort values (`any`→`string` defaulted to `""`), and the ticket-product-modal region `forEach` (`AdminRegion`). An `order-remaining-orders-group-section` map and the `product-additional-attribute-section` form/attribute anys were attempted/assessed and left (cascade-prone: the typed order-set `items` lack `subtitle`; the section uses `useForm<any>` + custom `attribute_values`). Remaining 476: ~383 `no-explicit-any`, 82 `exhaustive-deps`, 7 `no-unused-vars`, 4 a11y. **Step 13 done 2026-06-22**: ratcheted cap 476 → **467** by clearing 9 more genuinely-safe `no-explicit-any` (typecheck-gated, 0 errors) — `attributes/attribute-list` sort values (`any`→`string`), `create-ticket-product-modal` price object (inline `{ currency_code; amount; rules? }` type), the promotions `rules-form-field` `fields` callbacks (inferred from `useFieldArray`), and the `pricing-edit` defaultValues `variant`/`price`/`acc` reduce (typed via `AdminProductVariant` inference + a `Record<string,number>` accumulator + an `AdminPrice & { rules? }` cast for the API's region-pricing field). Remaining 467: ~374 `no-explicit-any`, 82 `exhaustive-deps`, 7 `no-unused-vars`, 4 a11y. **Step 14 done 2026-06-22** (SDK-typing sprint, TS-1 slice 1): ratcheted cap 467 → **445** by replacing the load-bearing `UseMutationOptions<any, FetchError, any>` generics in the admin data-fetch hooks with precise Medusa SDK response/payload types (`hooks/api/{orders,products,fulfillment,order-edits}.tsx` — 11 mutation hooks, plus the `useCreateFulfillment` `payload: any`), clearing 22 `no-explicit-any` (typecheck-gated, 0 errors). The stricter contracts surfaced three latent call-site gaps, fixed behavior-equivalently: the fulfillment cancel `mutateAsync(undefined)` → `mutateAsync({})`, the product-option create `values` defaulted to `[]`, and the variant create `prices`/`inventory_items` `.filter(Boolean)` → type-guard predicates so the narrowed element types match the SDK request shapes. Remaining 445: ~352 `no-explicit-any`, 82 `exhaustive-deps`, 7 `no-unused-vars`, 4 a11y. | `admin-panel/package.json` `lint` script, `admin-panel/.eslintrc.json` | 445 cap (currently 445 warnings) | admin-panel team | M (down from L) | `v1.0.0` → `v1.2.0` |
| ~~LR-2~~ | ~~Eliminate vendor-panel typecheck failures~~ — **resolved**: `pnpm typecheck`, `pnpm test`, `pnpm build:preview`, `pnpm lint --max-warnings 0` all pass on `main` as of 2026-05-06; `continue-on-error: true` was correctly removed in PR #658. | `vendor-panel/**` | 0 errors | — | — | done |
| ~~LR-3~~ | ~~Eliminate type drift in admin-panel typecheck.~~ **Fully resolved 2026-05-13** (10 sub-passes, 710 → **0** errors). Pass 10 cleared the residual 164: `promotions/**` (53 → 0) by introducing `RuleResponseExt` alias + casting around `AdminPromotionRule` join shape, widening `RuleTypeKey` to accept hyphen + underscore variants, casting `setValue` keys to `any` for dynamic template-literal paths; `products/**` (40 → 0) including `product-create-variants-form` (typed `originalIndex` annotation cast), `pricing-edit` (rules cast through `{ rules?: Record<string, string> }`), `product-list-table/use-product-table-filters` (cast `tag_id`/`sales_channel_id` accessors), `product-table-adapter` (React-Query v5 `previousQuery.queryKey` indexing), and three null-guards in `product-edit-option`; `product-variants/**` (32 → 0) via a relaxed `VariantInventoryRow` shape that survives the SDK intersection collapse, plus a relaxed `ExtendedInventoryItem` for the inventory table columns; and the 19-error residual tail (lib/sdk env extensions in `vite-env.d.ts`, `query-key-factory` unknown-cast for the readonly tuple return, `table-display-utils` Badge variant migration, lib/tenancy/context return type annotation, donations/categories/sales-channels metadata pages id-non-null assertions and `mutateAsync as any` for `MetaDataSubmitHook` variance, campaigns `value ?? undefined` for null-vs-undefined Input prop, `LotAllocationLabels` enum-key fix, `replaceAll` → `split.join` workaround for ES2020 target, `DigitalProduct`/`MediaType` types added to `src/types/index.ts`). Lint remains at **3985 warnings / 0 errors** (under 4000 cap). The admin-panel `pnpm typecheck` step in `.github/workflows/ci.yml` can now be flipped to fail-fast (`continue-on-error: false`) in a follow-up commit on this branch. | `admin-panel/**` | 0 errors (was 710) | admin-panel team | ✅ done | `v1.0.0` |
| ~~LR-4~~ | ~~Replace backend `lint` (aliased to `tsc --noEmit`) with a real ESLint flat config that lints `src/**/*.{ts,tsx}` with `--max-warnings 0`~~ — **resolved 2026-06-14**: new `backend/eslint.config.mjs` (ESLint 9 flat config + unified `typescript-eslint`, mirroring `vendor-panel`), `lint` now = `eslint src --max-warnings 0` (+ `lint:fix`); `typecheck` keeps `tsc --noEmit`. Pragmatic rule tier: `eslint:recommended` + `typescript-eslint` recommended, with `no-explicit-any` parked (see TS-3), `no-require-imports` off (intentional dynamic framework requires), and `no-useless-catch` off (low-value, 13 control-flow sites). Fixed 157 violations to reach 0: 109 unused vars/imports, 18 case-declarations, 6 ban-ts-comment (5 stale `@ts-ignore` removed), 3 empty-object-types, plus prefer-const/useless-escape/empty-pattern/triple-slash/irregular-whitespace. Wired into CI (`.github/workflows/ci.yml` "Lint backend"). Backend typecheck 0 errors; 733 unit tests green. | `backend/package.json`, `backend/eslint.config.mjs` | 0 | — | — | done |
| ~~LR-5~~ | ~~Eliminate storefront `pnpm typecheck` failures.~~ — **resolved 2026-05-13**: installed `sonner@^2.0.7`; inlined `NextFetchRequestConfig` shape after Next 15.5.15 removed the public export; widened `MedusaFetchOptions.body` to `Record<string, unknown>` and `query` value type to include `boolean` (covers `with_deleted`, `shipping_method_ids`, `promo_codes`, `reference`, `action` body fields); coerced `null` returns from `getAuthHeaders()` to `undefined` in `src/lib/data/orders.ts`; gated `apiProducts` access with `?.` / `?? []`; added `verified?: boolean` to `SellerProps`; added missing `Style` export to `src/types/categories.ts`; switched `SellerScheduling.tsx` to `import type` to satisfy `isolatedModules`; narrowed `err: unknown` → `err instanceof Error ? err.message : String(err)` in `src/lib/data/customer.ts`; null-coalesced `res.error` in `PaymentButton.tsx` and `AddressForm.tsx`; moved `headers` to the 4th `deleteLineItem` arg (SDK now takes `SelectParams` 3rd); fixed `@type/categories` → `@/types/categories` path. `pnpm typecheck` now passes; `ci.yml` Storefront typecheck step is fail-fast. | `storefront/src/lib/{data,helpers,config.ts}`, `storefront/src/components/sections/{CartReview,ProductFeed,ProductListing,SellerScheduling,ShopByStyle}/**`, `storefront/src/types/{seller,categories}.ts` | 0 type errors | — | — | done |

## Storefront test coverage

| # | Item | Location | Owner | Effort | Target milestone |
|---|------|----------|-------|:------:|------------------|
| ~~TC-1~~ | ~~Bring storefront test suite to ≥ 30 % coverage on `src/lib/data/*` and `src/lib/helpers/*`.~~ — **resolved 2026-06-22**. First pass gated `src/lib/helpers/**` at ≥30% (now **58% lines / 75% funcs**). Second pass (this one) added mocked-fetch unit suites for the data layer — `regions`, `categories`, `products`, `orders`, `customer`, `cart` (84 new tests) — mocking `@/lib/config` (`medusaFetch`/`fetchQuery`/`sdk`), `@/lib/data/cookies`, `next/cache`, `next/navigation`, and sibling data modules via `vi.hoisted` + `vi.mock` (sidesteps the `"use server"` / `server-only` import barrier). `src/lib/data/**` is now at **41% lines** (86% funcs). The vitest coverage `include` covers both layers with per-glob thresholds (helpers lines+funcs ≥30, data lines ≥30); `pnpm test:coverage` gates green. | `storefront/src/lib/data/__tests__/*`, `storefront/vitest.config.ts` | storefront team | M | done |

## Storefront QA

| # | Item | Source | Owner | Effort | Target milestone |
|---|------|--------|-------|:------:|------------------|
| ~~QA-1~~ | ~~Add recurring static internal-link route validation in QA/release checks~~ — **resolved 2026-05-13**: `scripts/release_validation.sh` now invokes `pnpm qa:internal-links` in the storefront block (already wired in `.github/workflows/ci.yml` as a hard gate); release validation will now flag dead internal hrefs alongside other storefront checks. | `scripts/release_validation.sh`, `storefront/scripts/check-internal-links.js` | — | — | — | done |

## Test infrastructure

| # | Item | Location | Owner | Effort | Target milestone |
|---|------|----------|-------|:------:|------------------|
| ~~TI-1~~ | ~~Reconcile backend module migration order so `pnpm test:integration:http` can run end-to-end.~~ — **resolved 2026-05-13** (narrow scope: hawala-ledger raw-columns ordering + payout-breakdown base table). Gate briefly flipped to fail-fast 2026-05-17 and reverted to `continue-on-error: true` the same day after the first live-Postgres run surfaced a deeper chain captured as **TI-3**. The narrow TI-1 fix is intact (those specific migrations work); the gate stays non-blocking until TI-3 closes. | `backend/src/modules/{hawala-ledger,payout-breakdown}/migrations/*.ts` | backend team | M | `v1.0.0` |
| ~~TI-2~~ | ~~Harden the `e2e.yml` Playwright job and flip it to fail-fast.~~ — **resolved 2026-06-20** (production-readiness pass): both `continue-on-error: true` lines removed (the "Bring up the stack" and "Run Playwright tests" steps now block CI). Cold-build flakiness mitigated by: `docker/setup-buildx-action` (BuildKit) + `DOCKER_BUILDKIT=1`/`COMPOSE_DOCKER_CLI_BUILD=1`, a base/infra image pre-pull step (node/nginx/postgres/redis/minio), and a wider per-service healthcheck wait window (60→90 × 5s) that now `exit 1`s with `::error::` if a service never goes healthy; `timeout-minutes` 35→45. The diagnostic `docker compose logs` dump on failure is retained. Optional future refinement: cross-run GHA layer cache via `compose bake` (needs `cache_from`/`cache_to` in the shared `docker-compose.yml` build sections) — deferred to avoid touching the prod/Fedora compose contract. Backend boot fixed by supplying a dummy `STRIPE_API_KEY` in compose (the `@mercurjs/b2c-core` PayoutProvider crashed on an empty key). The one failing Playwright test was a test bug — it called `/v1/integrations/blackout/entitlements/economic-standing?mxid=…` (query) but the route is `…/economic-standing/{mxid}` (path param), so it 404'd; fixed to use the path param. | `.github/workflows/e2e.yml`, `docker-compose.yml`, `e2e/tests/vendor-verification.spec.ts` | — | — | done |
| TI-4 | **Fix landed 2026-06-22; awaiting Docker verification.** Root cause: in the compose backend, `medusa db:migrate --execute-safe-links` failed with `EACCES: permission denied, mkdir '/app/src/modules/local-delivery-fulfillment/migrations'` under the non-root `medusa` runtime user, so the Medusa link table `publishable_api_key_sales_channel` was never created and `pnpm seed` aborted (`TableNotFoundException`). **Fix:** `backend/Dockerfile` now pre-creates a `migrations/` dir inside every module (`for d in /app/src/modules/*/; do mkdir -p "$d/migrations"; done`) and re-asserts `chown -R medusa:medusa /app/src` before the `USER medusa` switch, so migrate never needs to mkdir at boot and is idempotent. The loop was verified against the real module tree (covers all 66 modules incl. `local-delivery-fulfillment`, idempotent, preserves existing migration files). **Still open:** a full image build + compose `db:migrate`/`seed` run could not be executed in the authoring env (no Docker daemon); confirm end-to-end on a runner before closing. | `backend/Dockerfile`, `backend/scripts/railway-start.js` | — | platform | `v1.1.0` |
| ~~TI-3~~ | **Flipped to fail-fast 2026-06-20** (production-readiness pass): the `continue-on-error: true` line on the `test-integration` step in `ci.yml` was deleted, so `pnpm test:integration:http` now blocks CI against the live Postgres service. Both documented code blockers (productsCreated hook conflict → route middleware; missing Stripe bootstrap keys) were already addressed (see below); the flip was made per the user's explicit "flip anyway" decision, accepting that CI's Postgres service is now the verification surface. If a deeper boot/migration chain surfaces on the runner (as when TI-1 was briefly flipped on 2026-05-17), the job will fail — fix forward rather than re-masking. The `test-soak` (money-path concurrency) step was flipped the same way in the same pass. Original notes: Get `pnpm test:integration:http` fully green against a live Postgres, then flip the `ci.yml` integration-test step to fail-fast. Five distinct issues surfaced on the first live-Postgres run on 2026-05-17 (branch `claude/prepare-live-deployment-I1EJ1`); the first four landed on that PR. **Remaining: observe one green live-Postgres run, then flip the gate to fail-fast** (delete `continue-on-error: true` on the integration-test step). Both documented code blockers are now addressed: (a) **Hook conflict** — *resolved*: listing-type validation no longer registers a `createProductsWorkflow.hooks.productsCreated` hook (which collided with `@mercurjs/b2c-core`'s single-handler registration); it now runs as a pre-commit route middleware `enforceListingTypeAllowed` (`backend/src/shared/listing-type-guard.ts`) wired on POST `/vendor/products` and `/vendor/seller-products` in `backend/src/api/middlewares.ts`. (b) **Missing test-env Stripe secrets** — *wired*: `STRIPE_SECRET_API_KEY=sk_test_dummy` and `STRIPE_API_KEY=sk_test_dummy` added to the integration-test env in `.github/workflows/ci.yml` so `@mercurjs/b2c-core`'s `PayoutProvider` Stripe bootstrap no longer aborts. The flip is intentionally deferred one cycle per the TI-1 lesson (flipped then reverted same day when a deeper chain surfaced) — confirm the job runs green against the live Postgres service first. **Landed in PR #677** (no change needed): payout-breakdown `payout_config` → `hawala_payout_config` table-name fix; product-archetype non-transactional migration ordering (enum values pre-declared in `Migration20251228CreateProductArchetype` so subsequent `ADD VALUE IF NOT EXISTS` migrations are idempotent no-ops within the master transaction); creator-attribution `Migration20260520CreateCreatorAttribution` → `Migration20260519CreateCreatorAttribution` rename (sorts before sibling Add migrations); creator-program `Migration20260520203AddReferralLevels` → `Migration20260616AddReferralLevels` rename (sorts after `Migration20260615CreateCreatorProgram`). | `.github/workflows/ci.yml`, `backend/src/workflows/hooks/**`, `backend/integration-tests/**` | backend team | M | `v1.0.0` |

## Security dependency bumps (HIGH/CRITICAL — gated, blocking)

All five rows below were resolved together on 2026-05-13 via the production-readiness pass on branch `claude/check-production-readiness-WGO1m`. `.trivyignore` was emptied at the same time and **the Trivy FS gate was flipped to fail-fast on 2026-05-17** (branch `claude/prepare-live-deployment-I1EJ1`, `.github/workflows/security.yml`). New HIGH/CRITICAL findings now block CI and must be triaged into `.trivyignore` with a fresh `SD-*` row here before the gate can be made green again.

| # | Package | Range | Fixed | CVEs | Affected lockfile | Owner | Effort | Target |
|---|---------|-------|-------|------|-------------------|-------|:------:|--------|
| ~~SD-1~~ | ~~`@mikro-orm/core` `<6.6.10`~~ — **resolved**: bumped to `6.6.10` across `@mikro-orm/{core,cli,knex,migrations,postgresql}` in `backend/package.json` (direct deps + pnpm.overrides) and root `package.json` on 2026-05-13. Lockfiles now have a single `@mikro-orm/core@6.6.10` graph. | `<6.6.10` → `6.6.10` | CVE-2026-34220, CVE-2026-34221 | `backend/pnpm-lock.yaml`, root `pnpm-lock.yaml` | — | — | done |
| ~~SD-2~~ | ~~`lodash` `<4.18.0`~~ — **resolved**: bumped to `^4.18.0` (resolves to `4.18.1`) in `backend/`, `storefront/`, `vendor-panel/`, `admin-panel/` package.json + pnpm.overrides on 2026-05-13. Runtime `lodash` is `4.18.1` everywhere; remaining `@types/lodash@4.17.x` is a types package, not exploitable. | `<4.18.0` → `4.18.1` | CVE-2026-4800 | `backend/`, `storefront/`, `vendor-panel/`, `admin-panel/pnpm-lock.yaml` | — | — | done |
| ~~SD-3~~ | ~~`picomatch` `<4.0.4`~~ — **resolved**: pinned via pnpm.overrides `>=4.0.4` (resolves to `4.0.4`) in `backend/`, `storefront/`, `vendor-panel/`, `admin-panel/package.json` on 2026-05-13. No 2.x copies remain at runtime. | `<2.3.2 / <3.0.2 / <4.0.4` → `4.0.4` | CVE-2026-33671 | `backend/`, `storefront/`, `vendor-panel/`, `admin-panel/pnpm-lock.yaml` | — | — | done |
| ~~SD-4~~ | ~~`axios` `<1.15.2`~~ — **resolved**: bumped to `^1.15.2` (resolves to `1.16.0`) via direct dep in `backend/package.json` and pnpm.overrides in `backend/`, `storefront/`, `vendor-panel/package.json` on 2026-05-13. | `<1.15.2` → `1.16.0` | CVE-2026-42033, 42035, 42043, 42264 | `backend/`, `storefront/`, `vendor-panel/pnpm-lock.yaml` | — | — | done |
| ~~SD-5~~ | ~~`next` `<15.5.15`~~ — **resolved**: bumped to `15.5.15` in `storefront/package.json` (dep + resolutions + pnpm.overrides) on 2026-05-13. | `<15.5.15` → `15.5.15` | GHSA-q4gf-8mx6-v5v3 | `storefront/pnpm-lock.yaml` | — | — | done |
| ~~SD-6~~ | ~~`lodash` `<4.18.0` and `picomatch` `<4.0.4` in **root** workspace lockfile~~ — **resolved 2026-05-17**: SD-2/SD-3 fixes had not propagated to the root `package.json` / `pnpm-lock.yaml` (only the four per-app lockfiles). Added `pnpm.overrides` to root `package.json` for `lodash >=4.18.0` and `picomatch >=4.0.4`; regenerated root `pnpm-lock.yaml`. | regression of SD-2/SD-3 | CVE-2026-4800, CVE-2026-33671 | root `pnpm-lock.yaml` | — | — | done |
| ~~SD-7~~ | ~~`@mikro-orm/knex` `<6.6.14`~~ — **resolved 2026-05-17**: bumped to `6.6.14` across `@mikro-orm/{core,cli,knex,migrations,postgresql}` in `backend/package.json` (direct deps + pnpm.overrides) and root `package.json`. | `6.6.10` → `6.6.14` | CVE-2026-44680 | `backend/pnpm-lock.yaml`, root `pnpm-lock.yaml` | — | — | done |
| ~~SD-8~~ | ~~`next` `<15.5.18`~~ — **resolved 2026-05-17**: bumped to `15.5.18` in `storefront/package.json` (dep + resolutions + pnpm.overrides). | `15.5.15` → `15.5.18` | CVE-2026-44573, 44574, 44575, 44578, 44579, 45109; GHSA-8h8q-6873-q5fj | `storefront/pnpm-lock.yaml` | — | — | done |
| ~~SD-9~~ | ~~`protobufjs` `<7.5.5`~~ — **resolved 2026-05-17**: pinned via `backend/package.json` `pnpm.overrides` `protobufjs >=7.5.5`. **CRITICAL** severity. | `7.5.4` → `7.5.5+` | CVE-2026-41242 (CRITICAL), 44289, 44290, 44291, 44293 | `backend/pnpm-lock.yaml` | — | — | done |
| ~~SD-10~~ | ~~`multer` `<2.1.0`~~ — **resolved 2026-05-17**: pinned via `backend/package.json` `pnpm.overrides` `multer >=2.1.0` (resolves to `2.1.1`). | `2.0.2` → `2.1.1` | CVE-2026-2359, 3304, 3520 | `backend/pnpm-lock.yaml` | — | — | done |
| ~~SD-11~~ | ~~`@opentelemetry/{exporter-prometheus,sdk-node}` `<0.217.0`, `immutable` `<3.8.3`, `fast-xml-parser` `<5.3.6`, `rollup` `<4.59.0`~~ — **resolved 2026-05-17**: pinned via `backend/package.json` `pnpm.overrides`. | various | CVE-2026-44902, 29063, 26278, 33036, 27606 | `backend/pnpm-lock.yaml` | — | — | done |
| ~~SD-12~~ | ~~`minimatch` `<3.1.3` (3.x line) and `<10.2.1` (10.x line)~~ — **resolved 2026-05-17**: pinned via `backend/package.json` `pnpm.overrides` with caret-bounded keys `"minimatch@3": "^3.1.3"` and `"minimatch@10": "^10.2.1"` to keep both major version lines bounded (a flat `>=3.1.3` override was replacing 3.x with 10.x, leaking the 10.x vulnerability through). | `3.1.2` → `3.1.5`, `10.1.1` → `10.2.5` | CVE-2026-26996, 27903, 27904 | `backend/pnpm-lock.yaml` | — | — | done |
| ~~SD-13~~ | ~~`path-to-regexp` `<0.1.13` (0.x line) and `<8.4.0` (8.x line)~~ — **resolved 2026-05-17**: pinned via `backend/package.json` `pnpm.overrides` with caret-bounded keys `"path-to-regexp@0": "^0.1.13"` and `"path-to-regexp@8": "^8.4.0"`. | `0.1.12` → `0.1.13+`, `8.3.0` → `8.4.2` | CVE-2026-4867, 4926 | `backend/pnpm-lock.yaml` | — | — | done |
| ~~SD-14~~ | ~~`@remix-run/router` `<1.23.2` in admin-panel~~ — **resolved 2026-05-17**: added `@remix-run/router >=1.23.2` to `admin-panel/package.json` `pnpm.overrides` (backend already had it). **Follow-up (same day)**: the override created a transitive break — `react-router-dom@6.20.1` imports `UNSAFE_getPathContributingMatches` which `@remix-run/router@1.23.2` no longer exports, failing the admin-panel Docker build. Bumped `react-router-dom` `6.20.1` → `6.30.1` in `admin-panel/package.json` (latest v6 line, pairs with `@remix-run/router@1.23.2`). | `1.13.1` → `1.23.2+`; `react-router-dom 6.20.1 → 6.30.1` | CVE-2026-22029 | `admin-panel/pnpm-lock.yaml` | — | — | done |
| ~~SD-15~~ | ~~LOW/MEDIUM Trivy backlog (post-PR-678): 31 findings across 14 packages, visible in Code Scanning as `trivy-fs` but not gating CI (`limit-severities-for-sarif: true` scopes the gate to HIGH/CRITICAL).~~ — **resolved 2026-05-17** on branch `claude/trivy-low-medium-cves-b9cOI`. **Landed**: `dompurify ^3.3.1 → ^3.4.0` (storefront, resolves 3.4.3), `next-intl ^3.26.3 → ^4.9.2` (storefront, resolves 4.12.0 — confirmed dead direct dep, zero `src/` references), `uuid ^11.0.5 → ^11.1.1` (storefront), `i18next 23.7.11 → ^25.2.1` + `i18next-http-backend 2.4.2 → ^3.0.5` (admin-panel + vendor-panel, coupled major bump; resolves 25.10.10 / 3.0.6), `nodemailer ^7.0.11 → ^8.0.5` (backend dep + vendor-panel override; resolves 8.0.7), `react-router-dom 6.30.1 → 6.30.2` (admin-panel). **New overrides** added: `qs >=6.14.2` (root + backend + storefront resolutions/overrides + admin-panel; resolves 6.15.2), `postcss >=8.5.10` (root + backend + storefront resolutions/overrides + admin-panel + vendor-panel; resolves 8.5.14), `esbuild >=0.25.0` (backend). **Backend cleanup**: kept `react-router-dom 6.20.1 → 6.30.2` (sub-package `backend/restaurant-marketplace/` consumes it; clears CVE-2025-68470 there). **CI**: also applied `limit-severities-for-sarif: true` to the `trivy-image` job (`.github/workflows/security.yml` lines 187-220) — same footgun PR-678 fixed for `trivy-fs`, dormant today (only fires on push to `main`/`release/**`) but would have bitten the next release. **Fell back to `.trivyignore`** (documented runtime decisions, MEDIUM severity, do not gate CI): (a) `@octokit/{plugin-paginate-rest, request, request-error}` — overrides at the task's suggested fixed versions pulled in plugin v14 against `@octokit/core@4.2.4` (shipped by `@octokit/rest@19.0.13`, used by `backend/src/shared/github-service.ts`), creating a runtime peer-mismatch. Cleaner fix is bumping `@octokit/rest` to v22 + `@octokit/auth-app` 6→8 (a runtime auth flow change) — out of scope; suppressed `CVE-2025-25288/89/90`. (b) `vite ^5.4.14 → ^8.0.5` — vite 8 switched to Rolldown which deprecates the `manualChunks: object` rollup API used in `admin-panel/vite.config.ts` and `vendor-panel/vite.config.ts`; `@vitejs/plugin-react@4.x` also caps peer at vite 6. Rewriting both vite configs + bumping the React plugin is a separate Rolldown migration workstream; suppressed `CVE-2026-39365` (vite) and `GHSA-67mh-4wv8-2f99` (esbuild via vite). (c) `ajv ^8.17.1 → ^8.18.0` (admin-panel + vendor-panel devDep) and the `ajv >=8.18.0` override (root + backend + admin-panel + vendor-panel) resolved to ajv@8.20.0 which crashed `@eslint/eslintrc` (v2.1.4 in admin-panel ESLint 8.57.1, v3.3.3 in vendor-panel ESLint 9.39.2) with `NOT SUPPORTED: option missingRefs. Pass empty schema with $id that should be ignored to ajv.addSchema.` — broke `Admin Panel Checks` + `Vendor Panel Checks` in CI on PR #679. ajv 8.18+ added stricter `addSchema` validation that's incompatible with eslintrc's legacy meta-schema loader; both eslintrc versions are still the latest on their major lines so there's no upstream fix. Reverted to `ajv ^8.17.1` (direct + override removed); suppressed `CVE-2025-69873` (MEDIUM) until eslintrc is patched upstream or both panels migrate to ESLint flat config. | mixed (see Package cell) | CVE-2026-41238, 41239, 41240, 0540 (dompurify); GHSA-39q2-94rc-95cp, cj63-jhhr-wcxv, cjmm-f4jc-qw8r, h8r8-wccr-v5f2 (dompurify); CVE-2026-40299, GHSA-4c35-wcg5-mm9h (next-intl); CVE-2026-41305 (postcss); CVE-2026-41907 (uuid); CVE-2026-41691 (i18next-http-backend); GHSA-vvjj-xcjg-gr5g, GHSA-c7w3-x93f-qmm8 (nodemailer); CVE-2025-68470 (react-router); CVE-2026-2391 (qs) | root + `storefront/`, `admin-panel/`, `vendor-panel/`, `backend/pnpm-lock.yaml` | — | — | done |
| ~~SD-16~~ | ~~New HIGH Trivy-FS findings appeared on `main` (vuln-DB drift) and broke the gate.~~ — **resolved 2026-06-14**. **Bumped via `pnpm.overrides`**: `esbuild >=0.25.0 → >=0.28.1` (backend; resolves 0.28.1) and **added** `esbuild >=0.28.1` to storefront (`pnpm.overrides` + `resolutions`) — clears `GHSA-gv7w-rqvm-qjhr`; **added** `@grpc/grpc-js >=1.14.4` (backend; resolves 1.14.4) — clears `CVE-2026-48068`/`CVE-2026-48069`. **Suppressed in `.trivyignore`** (not actionable): `activesupport` `CVE-2026-33176` is a Ruby gem reported only because `@stellar/js-xdr@3.1.2` ships a stray `Gemfile.lock` inside its npm tarball — FBM has no Ruby runtime and nothing to bump. Verified: `trivy fs --severity HIGH,CRITICAL` exits 0; backend + storefront typecheck clean. | `backend/package.json`, `storefront/package.json`, `.trivyignore` | 0 HIGH | — | — | done |
| ~~SD-17~~ | ~~New HIGH Trivy-FS findings from vuln-DB drift re-broke the gate: `form-data`, `protobufjs`, `vite`.~~ — **resolved 2026-06-17** via `pnpm.overrides` (all three were `fixed` status, so the gate trips under `ignore-unfixed: true`): **`form-data >=4.0.6`** (added to `backend`/`storefront`/`vendor-panel`; was 4.0.5 — `CVE-2026-12143`), **`protobufjs >=7.5.5 → >=8.4.1`** (backend; was 8.3.0 — `CVE-2026-48712`), **`vite ^6.4.3`** (backend; was 5.4.21 and the 5.x line has no in-line fix) **+ `vite ^7.3.5`** (storefront; was 7.3.2 — both `CVE-2026-53571`). Bounded to the lowest fixed major to avoid vite 8 (the SD-15 Rolldown/`@vitejs/plugin-react@4` blocker). Backend `vite` is only a transitive peer of `@medusajs/admin-bundler`/`@medusajs/admin-vite-plugin`/`@vitejs/plugin-react@4.7.0` (all resolve vite 6.4.3 with no unmet peers) and the admin build is disabled in `medusa-config`, so `medusa build` never runs the bundler; storefront `vite` is test/type tooling only (`vitest` + `@medusajs/types` peer), not in the Next build. Verified: `trivy fs --severity HIGH,CRITICAL --ignore-unfixed` exits 0; backend typecheck + `medusa build` + storefront/vendor-panel typecheck all clean. No `.trivyignore` change needed. | `backend/`, `storefront/`, `vendor-panel/package.json` + lockfiles | CVE-2026-12143, CVE-2026-48712, CVE-2026-53571 | 0 HIGH | — | — | done |
| ~~SD-18~~ | ~~`concurrent-ruby` HIGH via `@stellar/js-xdr` stray `Gemfile.lock`.~~ — **resolved by suppression** (row backfilled 2026-07-21; the `.trivyignore` entry predates it and referenced this tracker without a row): same non-actionable pattern as SD-16's `activesupport` — the `@stellar/js-xdr@3.1.2` npm tarball vendors a Ruby `Gemfile.lock`; FBM has no Ruby runtime and nothing to bump. Suppressed `CVE-2026-54904` in `.trivyignore` until upstream drops the file. | `.trivyignore` | CVE-2026-54904 | — | — | done |
| ~~SD-19~~ | ~~`axios` `>=1.15.2 <1.18.0` HIGH (`GHSA-gcfj-64vw-6mp9`) from vuln-DB drift broke the Trivy-FS gate on `main` (failing daily since 2026-07-15).~~ — **resolved 2026-07-21**: raised the SD-4 floor — direct dep `^1.15.2 → ^1.18.0` in `backend/package.json` and `pnpm.overrides`/`resolutions` `>=1.15.2 → >=1.18.0` in `backend/`, `storefront/`, `vendor-panel/package.json` (resolves to `1.18.1`; root lockfile was already at `1.18.1`; admin-panel's `axios@1.16.0` is dev-only reachability, which the prod-scoped gate ignores). Lockfile collateral limited to axios's own new deps (`https-proxy-agent`, `agent-base`) and a `hasown` patch bump. Verified: `pnpm audit --prod` clean of non-suppressed HIGH/CRITICAL in all three roots; the SD-20 local trivy run (same PR) confirmed axios clear in all five lockfiles; backend + vendor-panel typecheck clean. No `.trivyignore` change needed. | `backend/`, `storefront/`, `vendor-panel/package.json` + lockfiles | GHSA-gcfj-64vw-6mp9 | 0 HIGH | — | — | done |
| ~~SD-20~~ | ~~Same-day vuln-DB drift re-broke the gate on PR #750 after SD-19: `brace-expansion` 1.1.14 + 5.0.6 (`CVE-2026-13149`, HIGH, exponential-time DoS) and `js-yaml` 3.14.2 (`CVE-2026-59869`, HIGH, DoS via crafted YAML), all prod-reachable in `backend/pnpm-lock.yaml` only.~~ — **resolved 2026-07-21** via `backend/package.json` `pnpm.overrides` with major-bounded keys (the SD-12 lesson; `js-yaml` 3→4 is an API break): `"brace-expansion@1": "^1.1.16"`, `"brace-expansion@5": "^5.0.7"`, `"js-yaml@3": "^3.15.0"`, plus `"js-yaml@4": "^4.3.0"` (dev-only today, bounded pre-emptively). Lockfile diff touches only those four packages. Note for future drift triage: these CVEs did **not** appear in `pnpm audit --prod` (npm's advisory feed keys them differently than trivy-db) — audit-clean does not imply gate-green; run trivy itself (installable from the `aquasecurity.github.io/trivy-repo` deb pool when GitHub releases are unreachable). Verified: `trivy fs --scanners vuln --severity HIGH,CRITICAL --ignore-unfixed` with CI's exact skip-dirs/ignorefile exits 0 across all five lockfiles; backend typecheck clean. No `.trivyignore` change needed. | `backend/package.json` + lockfile | CVE-2026-13149, CVE-2026-59869 | 0 HIGH | — | — | done |
| ~~SD-21~~ | ~~Next-day vuln-DB drift re-broke the gate on PR #752: `@opentelemetry/propagator-jaeger` 2.7.1 (`CVE-2026-59892`, HIGH, DoS via malformed HTTP header decoding) and `immutable` 5.1.5 (`CVE-2026-59879`/`CVE-2026-59880`, HIGH) in `backend/pnpm-lock.yaml`; `sharp` 0.34.5 (`GHSA-f88m-g3jw-g9cj`, HIGH, inherited libvips CVEs) in `storefront/pnpm-lock.yaml`.~~ — **resolved 2026-07-23** via `pnpm.overrides`: **backend** `immutable >=3.8.3 → >=5.1.8` (resolves 5.1.9; only the 5.x line is installed) and **added** `@opentelemetry/propagator-jaeger >=2.9.0` (resolves 2.10.0, joining the existing otel override rows); **storefront** added the range-bounded key `"sharp@^0.34.0": ">=0.35.0"` (overrides + resolutions) so Next 15's prod image-optimization copy bumps 0.34.5 → 0.35.3 while Storybook's dev-only `sharp@0.33.5` (unflagged; Trivy suppresses dev deps) stays untouched. Verified: local trivy 0.72.0 with CI's exact flags exits 0 across all five lockfiles; backend + storefront typecheck clean. No `.trivyignore` change needed. | `backend/`, `storefront/package.json` + lockfiles | CVE-2026-59892, CVE-2026-59879, CVE-2026-59880, GHSA-f88m-g3jw-g9cj | 0 HIGH | — | — | done |
| ~~SD-22~~ | ~~Vuln-DB drift re-broke the gate on PR #754: `next` 15.5.18 (`CVE-2026-64641` DoS + `CVE-2026-64645`/`CVE-2026-64649` SSRF, HIGH) in `storefront/pnpm-lock.yaml`; `postcss` 8.5.14/8.5.16 (`GHSA-r28c-9q8g-f849`, HIGH, path traversal in source-map auto-loading) in `backend`/root/`storefront`; `brace-expansion` 1.1.16 + 5.0.7 (`CVE-2026-14257`, HIGH, DoS — supersedes SD-20's `CVE-2026-13149`, fixed only in 5.0.8) in `backend`; and a second `concurrent-ruby` advisory (`CVE-2026-54906`, ReadWriteLock synchronization flaw) via the same `@stellar/js-xdr@3.1.2` stray `Gemfile.lock` as SD-16/18.~~ — **resolved 2026-07-25** via `pnpm.overrides`: **next** `15.5.18 → 15.5.21` (`storefront/package.json` dep + override); **postcss** `>=8.5.10 → >=8.5.18` (root + `backend` + `storefront`; resolves 8.5.18); **brace-expansion** kept on SD-20's major-bounded split (`"brace-expansion@1": "^1.1.16"`, `"brace-expansion@5": "^5.0.8"`) with `@5` raised 5.0.7→5.0.8 to fix the named-export line. `CVE-2026-14257` is fixed **only** in 5.0.8 — the `@isaacs` rewrite that changed the export from `module.exports = expand` to a named export — and the maintainer did not backport to the function-export line (1.x tip 1.1.16, 2.x tip 2.1.2 both still flagged). That function-export line is required by `minimatch@3.1.5` (pinned by `@eslint/config-array` and `test-exclude`); a first attempt to collapse the split to a catch-all `"brace-expansion": ">=5.0.8"` forced those onto 5.0.8 and threw `TypeError: expand is not a function`, red-lighting `Lint & Type Check` on PR #754 (the unit suite + `medusa build` passed because they use `minimatch@10`, which wants the new API — so lint, not tests, is the guard here). Reverted to the split and **suppressed** `CVE-2026-14257` in `.trivyignore` (SD-15-style: only fix breaks dev/CI tooling; the 1.x copy is reached only by ESLint/coverage globbing, not a production request path). Also **suppressed** `CVE-2026-54906` (same non-actionable `@stellar/js-xdr` Ruby `Gemfile.lock` as SD-16/18; extends the SD-18 entry). Verified: local trivy 0.72.0 with CI's exact flags (`fs --scanners vuln --severity HIGH,CRITICAL --ignore-unfixed --ignorefile .trivyignore`) exits 0 across all lockfiles; **backend lint** + typecheck + full unit suite (1098) + `medusa build`, storefront typecheck + tests (226), portals build all clean. | `backend/`, `storefront/package.json` + root + lockfiles, `.trivyignore` | CVE-2026-64641, CVE-2026-64645, CVE-2026-64649, GHSA-r28c-9q8g-f849, CVE-2026-14257, CVE-2026-54906 | 0 HIGH | — | — | done |
| ~~SD-23~~ | ~~Vuln-DB drift surfaced three pre-existing HIGH advisories in `storefront/pnpm-lock.yaml`: `brace-expansion` 5.0.6 (`CVE-2026-13149`, DoS), `fast-uri` 3.1.0 (`CVE-2026-6321`/`6322`/`13676`/`16221`), and `serialize-javascript` 6.0.2 (`GHSA-5c6j-r48x-rmvq`).~~ — **resolved 2026-07-27** via `storefront/package.json` `pnpm.overrides`: `"brace-expansion@5": "^5.0.8"` (5.x line only — no `minimatch@3` function-export consumer in the storefront, so no SD-22-style split needed), `"fast-uri": "^3.1.4"` (bounded to the 3.x line that `ajv` expects rather than letting the override jump to 4.x), `"serialize-javascript": ">=7.0.3"` (the GHSA is fixed only on the 7.x line; it is a build-time terser dep with a stable API). Surfaced while adding the storefront Sentry SDK but confirmed **pre-existing** — present in `storefront/pnpm-lock.yaml` at PR #755 head `37cb9ba` before the dep add — i.e. genuine drift, not introduced by `@sentry/nextjs`. Verified: local trivy 0.72.0 with CI's exact flags exits 0 across all lockfiles; storefront typecheck + tests (226) + a full `next build` all clean. No `.trivyignore` change needed. | `storefront/package.json` + lockfile | CVE-2026-13149, CVE-2026-6321, CVE-2026-6322, CVE-2026-13676, CVE-2026-16221, GHSA-5c6j-r48x-rmvq | 0 HIGH | — | — | done |
| ~~SD-24~~ | ~~`.trivyignore.image` suppressed three HIGH advisories vendored inside the pnpm distribution shipped in the backend runtime image: `brace-expansion` 2.1.2 (`CVE-2026-69152`), `ip-address` 10.2.0 (`CVE-2026-69192`) and `pnpm` 10.34.5 itself (`CVE-2026-55697`). Reachable by neither `pnpm.overrides` nor a lockfile change, and pnpm cannot be removed the way npm was because `scripts/railway-start.js` boots the server with `pnpm exec medusa start`.~~ — **resolved 2026-08-04** by Node 20 -> 22 plus pnpm 10.34.5 -> 11.20.0. The fix is better than the version bump implies: pnpm 11 vendors **23** packages against 10.34.5's **80**, and `brace-expansion` and `ip-address` are not among them at all (verified against both corepack caches — the same tree the image scan reads — `dirs=1/1` on 10.34.5 vs `0/0` on 11.20.0). All three removed from `.trivyignore.image`; `CVE-2026-14257` stays, being SD-22's separate finding in our own tree.
| ~~SD-25~~ | ~~Vuln-DB drift broke the Trivy filesystem gate on `main` before PR #849, and stayed broken across the #846, #847 and #848 merges. Six advisories, two of them CRITICAL and both unauthenticated RCE: `next` 15.5.21 in `storefront/pnpm-lock.yaml` (`CVE-2026-75604`, RCE on Windows-hosted servers; `GHSA-2xp9-vwfh-vxw4`, RCE in the Image Optimization API on crafted AVIF input), `sharp` 0.35.3 (`GHSA-rgj7-g3m4-5g8c`, bundled libheif), and in `backend/pnpm-lock.yaml` `js-yaml` 3.15.1 (`CVE-2026-84375`, parser DoS), `multer` 2.2.0 (`CVE-2026-77037` fd leak on aborted upload, `CVE-2026-77078` crafted multipart field name, `CVE-2026-82333` oversized array index — all reachable, multer parses untrusted multipart bodies) and `nodemailer` 9.0.1 (`GHSA-2x7j-588g-ccc2`, quadratic addressparser).~~ — **resolved 2026-09-09** on branch `claude/bmc-transmutation-strategy-6jenal`. All six had fixed releases, so nothing was suppressed and `.trivyignore` is unchanged. Floors raised in the two `pnpm-workspace.yaml` override blocks (pnpm 11 ignores `pnpm.overrides`) and mirrored in `package.json`: `next` 15.5.21 → **15.5.24** (exact pin, as before), `sharp@^0.34.0` `>=0.35.0` → **`>=0.35.4`**, `js-yaml@3` `^3.15.1` → **`^3.15.2`**, `js-yaml@4` `^4.3.1` → **`^4.3.2`**, `multer` → **`^2.3.0`**, `nodemailer` → **`^9.1.0`**. Two of these were the SD-15 lesson repeating: `sharp`'s `>=0.35.0` and `js-yaml`'s `^3.15.1` both *permitted* the fixed version while the lockfile stayed on the vulnerable one — a permissive range is not a bump. And `multer`/`nodemailer` are the nanoid lesson: a first pass used bare `>=` floors and pnpm resolved `nodemailer` to **10.0.1**, an unreviewed major; both are now caret-bounded and resolve to 2.3.0 and 9.1.1. Verified: `trivy fs` with CI's exact flags reports **0 across all six lockfiles**; `scripts/check-override-coverage.mjs` passes on all 5 roots; backend `tsc --noEmit` clean and the full CI unit suite green (355 suites, 3517 tests); storefront `tsc`, `lint`, `qa:internal-links` and `next build` all clean on 15.5.24. | `storefront/`, `backend/` (`pnpm-workspace.yaml` + `package.json` + lockfiles) | CVE-2026-75604, GHSA-2xp9-vwfh-vxw4, GHSA-rgj7-g3m4-5g8c, CVE-2026-84375, CVE-2026-77037, CVE-2026-77078, CVE-2026-82333, GHSA-2x7j-588g-ccc2 | 0 HIGH/CRITICAL | — | — | done |
| ~~SD-26~~ | ~~The Trivy **image** gate (`docker-build.yml` `trivy-image`) has been red on `main` for the `admin-panel` and `vendor-panel` matrix legs, through the #846, #847, #848 and #849 merges. Both are the only two images whose runtime stage is `nginx:${NGINX_VERSION}` (1.31-alpine); the `backend` and `storefront` legs run on `node:22-bookworm-slim` and pass. Alpine 3.24.1 in that base ships `libuuid` 2.42.1-r0, carrying seven fixed HIGH util-linux advisories: `CVE-2026-53612` and `CVE-2026-53613` (TOCTOU in mount when applying post-mount ownership/mode), `CVE-2026-53614` (SUID mount nosuid/noexec bypass via `LIBMOUNT_FORCE_MOUNT2`), `CVE-2026-76642` (failed external mount helper still runs privileged X-mount post-hooks), `CVE-2026-78408` (`nsenter --join-cgroup` leaks root cgroup migration authority), `CVE-2026-78409` (X-mount.subdir detached-tree resolution escapes via intermediate symlinks) and `CVE-2026-78410` (restricted bind mounts do not pin the source).~~ — **resolved 2026-09-10**. Nothing is suppressed: `.trivyignore.image` is unchanged, because all seven are fixed upstream. **A tag bump does not help** — `nginx:alpine`, `nginx:1.31-alpine` and `nginx:stable-alpine` were each scanned and all three carry the identical stale `libuuid` 2.42.1-r0, so upstream has simply not rebuilt against the fix. Alpine v3.24 does publish it (2.42.3-r1, at or above the 2.42.3-r0/r1 trivy names), so the runtime stage of both panel Dockerfiles now upgrades that one package: `apk add --no-cache wget tini && apk upgrade --no-cache libuuid`. **Scoped to `libuuid` on purpose, not a bare `apk upgrade`**: nginx and its five modules in this base come from NGINX Packaging (F5, `nginx-packaging@f5.com`), a different repo from Alpine main, so a bare upgrade would move nginx off whatever the `1.31-alpine` tag pins — the one thing the pinned tag exists to prevent. `libuuid`'s maintainer is Alpine main (Natanael Copa), so upgrading it cannot reach the nginx line. Verified by scanning the base image with the gate's exact flags and by confirming 2.42.3-r1 in the Alpine v3.24 package index; the built image itself could not be scanned from the dev container, which has no Docker daemon and is denied anonymous GHCR pulls. | `admin-panel/Dockerfile`, `vendor-panel/Dockerfile` | CVE-2026-53612, CVE-2026-53613, CVE-2026-53614, CVE-2026-76642, CVE-2026-78408, CVE-2026-78409, CVE-2026-78410 | 0 HIGH/CRITICAL | — | — | done |

**Node came first, and separately.** pnpm 11 requires Node >= 22.13 while CI pinned `NODE_VERSION: '20'` and all four Dockerfiles pinned `node:20-bookworm-slim`. A first attempt (#787) bumped only pnpm and failed every job with `ERR_UNKNOWN_BUILTIN_MODULE: No such built-in module: node:sqlite`; local verification had missed it because the dev shell runs Node 22. The runtime move is therefore its own commit, verified on Node 22 against the unchanged pnpm 10 lockfile before the package-manager change was layered on.

**The hazard the pnpm upgrade carries.** pnpm 11 stops reading `pnpm.overrides` from `package.json` — 84 of them across 5 roots, nearly all pinning a CVE fix. That case warns. The dangerous case is silent: overrides in `pnpm-workspace.yaml` are ignored **entirely** when `--ignore-workspace` is passed, with no diagnostic, and every CI install passed that flag. Reproduced on a scratch project: `minimatch` resolved to the vulnerable **3.0.0** under the flag despite `overrides: minimatch@3: ^3.1.3`, and **3.1.5** without it — and such a tree still passes Trivy, which reads the lockfile and would report the reverted versions as intended. Migration accordingly: overrides to a per-root `pnpm-workspace.yaml` (each app its own workspace root, which is what makes the flag unnecessary), `--ignore-workspace` removed everywhere, `pnpm-workspace.yaml` added to all four Dockerfile `COPY` lines, and an `allowBuilds` block per root since pnpm 11 makes an unadjudicated dependency build script a hard error (two deliberate refusals: `@medusajs/telemetry`, `@sentry/cli`).

`scripts/check-override-coverage.mjs` (#787) guards exactly this and runs before the Trivy filesystem scan. Verified semantically neutral: only `backend/pnpm-lock.yaml` changed, **1761 packages before and 1761 after with zero version-level changes** — the diff is entirely pnpm 11's peer-resolution key encoding — and lockfiles stayed at `lockfileVersion: '9.0'`. Also corrects the `SD-23` label collision and missing AUDIT_DEBT row that #781 left behind. | `.github/workflows/{ci,security}.yml`, all 4 `Dockerfile`s, `package.json` + `pnpm-workspace.yaml` x 5 roots, `backend/pnpm-lock.yaml`, `.trivyignore.image` | CVE-2026-69152, CVE-2026-69192, CVE-2026-55697 | 0 HIGH | — | — | done |

## Security hardening (production-readiness pass 2026-06-20)

Closes the gaps from the security review. All landed on branch
`claude/fbm-production-readiness-h4lywz`.

| # | Item | Resolution | Location |
|---|------|------------|----------|
| ~~SH-1~~ | Centralized error-response sanitizer (prevent field/query/stack leakage in API errors) | **done** — new `sanitizedErrorHandler()` wraps Medusa's canonical `errorHandler` (reuses its error-type→status mapping, no drift) and, **in production only**, rewrites `status >= 500` response bodies to a generic message while logging the full error via the structured logger and forwarding to Sentry. 4xx client errors (validation / not-found / auth) pass through unchanged; dev/test stays verbose. Wired as `errorHandler` in `defineMiddlewares`. Unit-tested (prod 5xx redaction, 4xx passthrough, dev verbosity). | `backend/src/shared/error-sanitizer.ts`, `backend/src/api/middlewares.ts`, `backend/src/shared/__tests__/error-sanitizer.unit.spec.ts` |
| ~~SH-2~~ | Explicit session-cookie timeout + flags | **done** — added `projectConfig.cookieOptions` (`httpOnly`, `secure` in prod, `sameSite: "lax"`, `maxAge` 24h) and `projectConfig.sessionOptions.ttl` (24h) so the admin/vendor `connect.sid` session cookie is hardened and no longer lives indefinitely. `sameSite: lax` is safe for the subdomain layout (admin/api share the `freeblackmarket.com` site); CSRF guard still enforces same-origin on writes. | `backend/medusa-config.ts` |
| ~~SH-3~~ | Sentry PII/secret redaction | **done (strengthened)** — the existing `beforeSend` already stripped `authorization`/`cookie`/`x-api-key`; broadened to a case-insensitive header allowlist (adds `set-cookie`, `x-medusa-access-token`, `x-publishable-api-key`), drops parsed `request.cookies`, and redacts token/secret/api_key/password query-string params. | `backend/src/shared/sentry.ts` |
| SH-4 | helmet.js | **DECISION: not adopted.** Security headers are already hand-rolled and complete in `securityHeadersMiddleware` (CSP per route class, HSTS in prod, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, no-store on admin/vendor). Adding `helmet` would duplicate/conflict with the existing per-route CSP logic for no security gain. Revisit only if the hand-rolled set drifts out of maintenance. | `backend/src/api/middlewares.ts` |

## D8 — Duplication across the ask/in-kind surfaces (2026-09-09)

**What D8 means.** `REPO_CONSOLIDATION_REVIEW.md`'s decision table defines D8 as
the hygiene/consolidation decision: duplication executed on where safe, queued
where riskier. This section is the queued half for one family of duplication
that the CDFI/co-op roadmap (§3.8) asked to have recorded rather than resolved.
Nothing here is a bug; each entry is a place where two or more systems model the
same real thing with different vocabularies, and where a future consolidation
has to pick one.

Found while building the FBM→Blackout mutual-aid seam, and **verified against
the code rather than transcribed** — the roadmap said "three in-kind intake
vocabularies" and there are four.

| # | Item | Status | Location |
|---|------|--------|----------|
| D8-1 | **Four "ask" stores across two repos, no shared vocabulary.** FBM `mutual_aid_request` (a person asks for help; OPEN/MATCHED/FULFILLED/WITHDRAWN/EXPIRED) · FBM `demand_post` (a group asks for supply; OPEN/THRESHOLD_MET/NEGOTIATING/EXPIRED) · Blackout `coalition_aid_posts` (need\|offer; open/in_progress/fulfilled/expired/cancelled) · Blackout `coalition_needs` (a coalition asks for a thing; open/claimed/fulfilled/closed). Four status vocabularies for four shapes of the same verb, and the FBM→Blackout mirror already has to map one onto another at the seam. **Near-miss to keep out of it:** FBM's `request` module is an RFQ/quote table whose docstring name-drops mutual aid; it is a buyer→vendor price request and shares no vocabulary with any of the above. | **deferred (L)** | `backend/src/modules/{mutual-aid,demand-pool}/models/`, blackout `packages/api/src/db/migrations/{020,054}_*.up.sql` |
| D8-2 | **Four in-kind intake vocabularies, only one of which records provenance.** FBM `intake_receipt` has a real taxonomy — `source` (donation/rescue/gleaning/overproduction/transfer_in), `donor_type` (individual/business/farm/organization/anonymous), plus `estimated_value_cents` with a `valuation_basis`. FBM `mutual_aid_offer.category` is free text with no source or valuation concept at all. Blackout `coalition_aid_posts.category` is a closed set of ten (food/transport/labor/materials/care/housing/childcare/eldercare/tech_support/other). Blackout `coalition_needs.kind` is free TEXT with a seven-value `SUGGESTED_NEED_KINDS` UI hint. The mirror maps FBM's free text onto Blackout's closed ten with an `other` fallback, which is the safe direction but is lossy and one-way. | **deferred (M)** | `backend/src/modules/aid-network/models/intake-receipt.ts`, `backend/src/modules/mutual-aid/models/mutual-aid-offer.ts`, blackout `packages/core/src/coalition/{mutualAid,needs}.ts` |
| D8-3 | **Two ways to record value that did not move as cash, neither aware of the other.** `RAIL_REGISTRY.GIFT` is `closed_loop: true` with `account_type: null` — it deliberately carries no balances — so the barter-accept route writes a zero-amount `TRANSFER` tagged `metadata.intended_rail: "GIFT"` and says so in a comment, because `createTransfer` takes no `currency_code` and GIFT-denominated accounts do not exist. Meanwhile `intake_receipt.estimated_value_cents` records an in-kind valuation for donor acknowledgment and contribution reporting, deliberately outside the money ledgers. Both are "value that is real but not cash"; they share no type, no unit, and no reporting path. Resolving means deciding whether GIFT gets an account type or whether in-kind valuation stays wholly outside the ledger — an accounting decision, not a refactor. | **deferred (L)** | `backend/src/modules/hawala-ledger/rails.ts`, `backend/src/api/store/collective/demand-pools/[id]/barter/[proposalId]/accept/route.ts`, `backend/src/modules/aid-network/models/intake-receipt.ts` |
| D8-4 | **Two mutual-aid surfaces in Blackout with the same name and no relation.** `apps/blackout-client/.../deaddrop/MutualAidPage.tsx` renders Matrix dead-drop "mutual aid threads" and never touches `coalition_aid_posts`; the Coalition MapTab is the only display consumer of the board. A reader looking for where mirrored asks appear will find the wrong file first. | **deferred (S)** | blackout `apps/blackout-client/src/app/features/{deaddrop,coalition}/` |

## D9 — Order-cycle sale accounting (2026-09-09)

Filed while giving `subscribers/order-cycle-order-placed.ts` its first producer.
That subscriber has always been complete — it records each sale against the
cycle product and links the order to the cycle — and nothing ever wrote the key
it reads, so it returned at its first guard on every order ever placed. Making
it fire for the first time makes the rows below **reachable**; none of them is
introduced by that change, and none is fixable without a decision.

| # | Item | Status | Location |
|---|------|--------|----------|
| D9-1 | **A cycle can oversell.** *(D9-2's idempotency removes duplicate-delivery as one cause, but not the concurrent-buyer race this row is about.)* `recordSale` writes `sold_quantity: sold_quantity + quantity` unconditionally — it never compares against `available_quantity`. `checkProductAvailability` now runs before an item enters the cart (`POST /store/order-cycles/:id/availability`), but that is a check, not a reservation: two buyers can pass it in the same instant and both orders record. A real fix is an inventory decision — reserve at add-to-cart, or fail the order at completion — not a guard, and the storefront cannot make it. | **deferred (M)** | `backend/src/modules/order-cycle/service.ts` (`recordSale`), `backend/src/api/store/order-cycles/[id]/availability/route.ts` |
| ~~D9-2~~ | ~~**`recordSale` is not idempotent and the subscriber does not dedupe.** A retried or duplicated `order.placed` double-increments `sold_quantity`. The sibling `subscribers/cottage-food-record-sale.ts` solves exactly this by making its own `recordSale` idempotent on `(source, source_id)`; order-cycle has no such key, so matching that pattern means a schema change.~~ — **fixed 2026-09-10.** The schema change this row anticipated: a new `order_cycle_sale` ledger keyed on `(order_cycle_id, variant_id, source, source_id)`, following cottage-food's shape so the two modules dedupe the same way. Per variant rather than per order, because one order can contain several variants from the same cycle and each is its own sale. **The guarantee is a partial unique index, not the service's read-then-write check** — two concurrent deliveries of the same event both pass that check, so uniqueness has to live in the database; a `23505` is caught and read as "already recorded". Partial on `source_id IS NOT NULL`, because a manual adjustment carries no source id and two identical manual corrections are two corrections, so that path stays deliberately non-idempotent. The ledger records `quantity` as well as the key, so `sold_quantity` can be rebuilt from it if it ever drifts. Additive migration: no change to `order_cycle_product`, nothing to backfill. Sales placed before the migration have no ledger row, so a duplicate delivery of a *pre-existing* order could still double-count once — accepted rather than backfilled, since D9-4's link is itself only recently populated. | done | `backend/src/modules/order-cycle/models/order-cycle-sale.ts`, `backend/src/modules/order-cycle/service.ts`, `backend/src/subscribers/order-cycle-order-placed.ts` |
| D9-3 | **A closed cycle still accepts sales.** The subscriber calls `retrieveOrderCycle` only to prove the cycle exists and never inspects `status`. This is arguably correct — an order placed while the cycle was open should count even if the sweep closes it moments later — but it is unstated behaviour rather than a decision, and there is no re-validation between add-to-cart and order-placed. Medusa v2 allows one handler per workflow hook and `completeCartWorkflow.hooks.validate` is already occupied by a composed FBM handler, so checkout-time re-validation must be added inside that file rather than alongside it. | **deferred (S)** | `backend/src/subscribers/order-cycle-order-placed.ts`, `backend/src/workflows/hooks/complete-cart-validate.ts` |
| ~~D9-4~~ | ~~**The order↔cycle remote link is write-only.** The subscriber creates it and nothing in the repo reads it. It is the natural source for the `ordersPlaced` field that FBM's §3 `cycle.close` event deliberately omits (`docs/contracts/blackout-integration.md`), which is now unblocked because the link finally has real rows in it.~~ — **fixed 2026-09-10.** `countCycleOrders` in `backend/src/lib/blackout-cycle.ts` counts the link rows for a cycle and the sweep attaches the result to `cycle.close`. Three choices worth keeping: it reads the **link** rather than the new `order_cycle_sale` ledger, because a sale row exists only for variants registered in the cycle while the link is written for every order tagged with it; it is sent on **close only**, matching Blackout's renderer, since a cycle that just opened has had no chance to take orders; and a failed read yields **`undefined`, not `0`** — Blackout drops the clause on `undefined` and prints "0 order(s) placed" on `0`, so collapsing the two would announce a confident falsehood in a vendor's room. A genuine zero is still sent as zero. The link is only recently populated, so cycles that ran before the storefront started tagging line items will under-report; not backfillable, since the tag was never written. | done | `backend/src/lib/blackout-cycle.ts`, `backend/src/jobs/order-cycle-status-update.ts`, `docs/contracts/blackout-integration.md` |
| ~~D9-5~~ | ~~**Cart metadata does not survive FBM's checkout.** `@mercurjs/b2c-core` overrides `POST /store/carts/:id/complete` with `splitAndCompleteCartWorkflow`, which builds its order payload by hand and never copies `cart.metadata` onto the orders — it fetches the field (`completeCartFields` includes it) and drops it. Line-item metadata it does carry. This is why the order-cycle producer writes to the line item. **Anything else in this repo that stamps cart metadata and expects to read it off the order is silently broken on the main checkout path** — `storefront/src/lib/data/donations.ts` and the cart `tier` route are the ones to audit first. FBM's other completion routes wrap Medusa's own `completeCartWorkflow` and do propagate, so behaviour differs by checkout path.~~ — **fixed 2026-09-10, and the audit it asked for was done.** `backend/src/lib/cart-metadata-recovery.ts` recovers a cart's metadata from any order it produced (order → `order_set` → `cart_id` → cart), because the drop is in a vendored workflow and this repo recovers rather than patches. Order metadata still wins per key; the cart read is skipped only when the order already carries **every** key asked for, since skipping on any one of them would let a partially-stamped order discard the rest. **The audit found three consumers, not one, and all three were silently broken on the main checkout path:** donations never accrued (`donation-order-accrued.ts`); last-click affiliate attribution never happened, so a creator whose link drove a sale earned nothing while promo-code attribution kept working off `order.promotions` and masked it (`attribute-order-on-placed.ts`); and every POS, vending and pickup sale resolved to the `online` default, which is worse than a missing number because the channel report looked complete (`attribute-channel-on-placed.ts`). The cart `tier` route the row named is **not** affected: nothing reads `order.metadata.tier`, and the per-item `sliding_scale_tier` stamp survives on line items regardless. | done | `backend/src/lib/cart-metadata-recovery.ts`, `backend/src/subscribers/donation-order-accrued.ts`, `backend/src/subscribers/attribute-order-on-placed.ts`, `backend/src/subscribers/attribute-channel-on-placed.ts` |
| D9-6 | **`link-booking-on-order-placed` can never fire.** The subscriber reads `order.metadata.booking_id` and its docblock says the key is "set by the embed checkout deep link". `GET /store/embed/bookings` only puts `booking_id` in a **URL query string** (`route.ts:111`), and nothing in the storefront reads that param back out onto the cart or the order — the string appears in exactly one place in the whole repo. So the booking is never linked to the order, never confirmed, and the customer never gets the confirmation email. Found while auditing D9-5; it is a different defect — an unfinished loop rather than metadata lost in transit — and the fix is storefront work, not recovery. | **deferred (M)** | `backend/src/subscribers/link-booking-on-order-placed.ts`, `backend/src/api/store/embed/bookings/route.ts` |

**The audit this row asked for was done, and it was worse than "silently broken" implies.** `storefront/src/lib/data/donations.ts` `setCartDonationPreferences` writes `donation_total`, `donation_beneficiary_id` and `storefront_id` onto the cart. `subscribers/donation-order-accrued.ts` read them off `order.metadata` on `order.placed`, found nothing, and hit its own `if (!beneficiaryId || donationTotal <= 0) return`. So **a buyer who chose to donate at checkout had their donation silently never accrued** — no error, no log, the beneficiary's `accrued_balance` simply never moved. A money-adjacent promise failing quietly is worse than one failing loudly.

Verified rather than inferred: the vendored `split-and-complete-cart.js` order payload was read directly and has **no `metadata` key at all** (line items and shipping methods carry theirs; the cart's is dropped).

Fixed by recovery, not by patching the dependency. The same workflow calls `createOrderSetStep({ cart_id: cart.id, ... })` and `order_set` is linked to its orders, so the originating cart is reachable: order → order_set → `cart_id` → `cart.metadata`. `lib/cart-metadata-recovery.ts` `getOrderCartMetadata` does that walk, lets the order's own metadata win on any key it carries (a correctly-propagating checkout path, or an operator edit, should not be overridden), skips both queries entirely when the order already has the data, and never throws — it runs inside an `order.placed` subscriber, where failing to recover an optional preference must not fail the order. A test reverting the subscriber to the old read confirms the regression is caught.

**Still open:** the drop itself, and any *other* consumer. `backend/src/api/store/carts/[id]/tier/route.ts` stamps cart metadata and has not been traced to a reader. New consumers should use `getOrderCartMetadata` rather than reading `order.metadata` directly. | **partly fixed (L)** | `backend/src/lib/cart-metadata-recovery.ts`, `backend/src/subscribers/donation-order-accrued.ts`, `backend/node_modules/@mercurjs/b2c-core/.medusa/server/src/workflows/cart/workflows/split-and-complete-cart.js`, `backend/src/api/store/carts/[id]/tier/route.ts` |

## D10 — Unauthenticated reads on the community `/store` surfaces (2026-09-10)

`api/middlewares.ts` `COMMUNITY_WRITE_PREFIXES` authenticates about fourteen
`/store/*` prefixes for `COMMUNITY_WRITE_VERBS` — POST, PUT, PATCH, DELETE —
and nothing else. Browsing those surfaces is meant to be public, so **GET was
never covered**. The prefix list gates *verbs, not data sensitivity*, and
several of those GETs return whole ORM entities by spreading them
(`...order`, `...delivery`, `...courier`, `...producer`) or project a
`customer_id` next to behavioural data. Two files make the gap visible in
place: `delivery-batches/[id]/route.ts` and `volunteer-logs/route.ts` each
enforce authorization in the write handler and none in the read handler
beside it.

Found while fixing D10-1. Ids are enumerable throughout: each prefix's own
unauthenticated list endpoint supplies them.

| ID | Finding | Status |
| --- | --- | --- |
| ~~D10-1~~ | ~~`GET /store/food-producers/:id/orders` — unauthenticated, no actor check, returning `recipient_name`, `recipient_phone`, `recipient_email`, `delivery_address_line_1/2`, `customer_notes` for every order of any producer. The sibling `orders/:orderId` route already checked ownership; only the list route was missed.~~ — **fixed 2026-09-10**: owner-only via a new `actorOwnsResource`, which unlike `actorMayManage` does **not** grandfather a null `owner_id`. Grandfathering is defensible for a write (middleware authenticates write verbs) but not for a read of a third party's address, where it would expose every legacy row to any account that can sign up. Both refusal paths return 403 so the response cannot confirm which producer ids exist. | done |
| ~~D10-2~~ | ~~`proof_pin_code` served on every delivery read.~~ — **fixed 2026-09-10**. The PIN is the secret a recipient reads back to a courier to confirm delivery (`food-deliveries/[id]/route.ts` and `[id]/proof/route.ts` both compare against it), so serializing it defeats the control: anyone holding it can confirm someone else's delivery. It is a credential, not data about the delivery. `modules/food-distribution/public-view.ts` `redactDelivery` strips it at the boundary — applied to the delivery list, the delivery detail, `delivery-batches/:id` and the `active_deliveries` embedded in `couriers/:id` — so it cannot be serialized to *any* caller, authorized or not, independently of how D10-5 is resolved. | done |
| ~~D10-3~~ | ~~`food_order.anonymous_recipient` never read by any path.~~ — **fixed 2026-09-10**. The column has always existed and no read consulted it, so a recipient who asked to be anonymous was returned with their name, phone, email and delivery address anyway. It matters most on `/store/food-donations`, whose rows are donation, gift, community-share, rescue and gleaning orders — food-aid recipients. `applyRecipientAnonymity` now blanks the identity fields and labels the recipient "Anonymous", applied on the donations and food-trades reads. The flag is a promise the schema makes to a person; the read path now keeps it. | done |
| ~~D10-4~~ | ~~`garden_vote.comment_visibility` selected but never applied.~~ — **fixed 2026-09-10**. `GET /store/proposals/:id/votes` selected the column and ignored it, so a comment a voter marked `private` was served to anonymous callers beside their `customer_id`. Only the comment is withheld — the tally still needs every vote, its weight and its voter, and a governance record that quietly dropped ballots would be worse than one showing a comment as withheld. `members_only` is treated as non-public because the route cannot yet tell a member from a stranger; when it can, that case can widen. | done |
| D10-5 | **The remaining unauthenticated reads, which need an access-model ruling rather than a patch.** Each returns personal data belonging to someone other than the caller: `GET /store/food-deliveries` and `/:id` (recipient name/phone, `delivery_address`, lat/long, `delivery_instructions`, `safe_place_description`, pickup contact); `/:id/track` and `/:id/subscribe` (live GPS breadcrumb plus the recipient's address — the SSE one continuously); `/store/food-trades` and `/:id` and `/store/food-donations` (the same `FoodOrder` field set as D10-1, through a different door); `/store/couriers` and `/:id` (`first_name`, `last_name`, `email`, `phone`, current lat/long, `license_plate`, `emergency_contact_name`/`_phone`, `documents`, `background_check_passed`, `total_earnings` — note `/:id/track` deliberately narrows the courier to name/vehicle/photo with the comment "Courier info (public only)", and the list endpoint has no such filter); `/store/delivery-batches/:id` (every delivery in the batch plus an explicit courier name and phone); `/store/volunteer-logs` (accepts `customer_id` as a filter, so one person's whole attendance history); `/store/work-parties/:id/signups` (attendance roster with check-in/out times); `/store/gardens/:id/members` (roster with `investment_balance`); `/store/harvests/:id/claims` (how much free food a named person took). **The ruling needed:** which of these are meant to be public at all, and for the rest, whether the reader must be the owner, a member of the same garden/network, or the subject. Not patched here because guessing the audience would either break a working surface or leave a hole. | open |
| ~~D10-6~~ | ~~`food_producer.hide_address` — declared with the comment "For cottage food / privacy — don't show exact address" and read by no path, so a cottage-food producer working from home has their street address and lat/long published.~~ — **fixed 2026-09-10.** Originally sorted into D10-5's "needs a ruling" bucket, which was wrong: `hide_address` is an *opt-in flag a producer set themselves*, so honouring it is not a guess about audience — it is the same shape as D10-3, and it belonged in the fix-now bucket from the start. `applyProducerAddressPrivacy` nulls `address_line_1/2`, `latitude` and `longitude` on `/store/food-producers` and `/:id`. City, state, postal code and country stay: the flag says *exact* address, and a local food marketplace still has to say roughly where a producer is. Coordinates are nulled rather than rounded — a rounded coordinate still reads as precise to every consumer of the field, and "how coarse is coarse enough" is a judgement nobody has made; a caller needing proximity can use the postal code. `service_area_radius_miles` is kept, since it describes how far someone travels, not where they live. | done |

**The pattern, for whoever takes D10-5.** Every one of these is a route whose
write verbs were locked down by the prefix matcher and whose read was not, and
the fixed rows split cleanly into two kinds. D10-2/3/4 needed no decision
because the schema *already* stated the intent — a PIN is a credential, an
anonymity flag is a promise, a visibility enum is a rule — and the read path
simply ignored it; those are bugs. D10-5 and D10-6 are different: the schema
says nothing about who may read, so fixing them means *choosing* an audience,
which is an operator decision and not a patch.

## Process

- Re-generate the in-code marker list with `rg -n "TODO|FIXME" admin-panel/src storefront/src vendor-panel/src` quarterly.
- Each item closed → strike-through here and link the merging PR.
- New audit findings should be appended here, not opened as ad-hoc files.
